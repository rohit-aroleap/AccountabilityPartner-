# Improvement Ideas — 2026-05-14

Generated by routine: accountability-partner-improvement
Codebase version: v1.047 · last commit: 9fe0651 v1.047: hide-from-AI (replaces v1.046 Periskope delete)
Source docs read: All JS modules (js/*.js), worker/worker.js, index.html, styles.css, README.md, git log (30 commits)
Additional docs: Ferra websites (ferra.club, rohit-aroleap.github.io/What_is_Ferra/) returned 403 — skipped. Local Ferra PDF files inaccessible from this environment — skipped.

## Summary
- Dashboard ideas: 118 across 12 categories
- Prompt engineering ideas: 99 across 14 categories
- Top emergent themes:
  1. **Data already collected, not used in prompts** — notes field, exercise names, progressive overload counts, monthly trends all sit in Firebase/workout export but never reach the system or user prompt
  2. **Operator-facing oversight gaps** — no search, no bulk actions, no dormancy sort; the list becomes unmanageable past ~20 customers
  3. **Prompt tightening beats prompt lengthening** — the prompts are solid but lack few-shot examples, a clear anti-pattern library, and hard format rules; each addition is low-effort and high-impact

---

## Dashboard (count: 118)

### 1. Customer management & onboarding
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 1 | Onboarding progress table | Add a read-only modal tab showing all customers with their `onboardingState`, `coachPersonality`, and `coachIntensity`, fetched from Firebase. Operators can see who's stuck mid-questionnaire without clicking each customer. | M | M |
| 2 | Bulk onboarding re-trigger | Currently `/onboarding/begin` in `add-customer.js:220` fires one customer at a time. Add a "Re-trigger for selected" checkbox action in the sidebar to re-run the endpoint for a list of phones at once. | M | M |
| 3 | Personality badge in customer-settings header | Move `coachPersonality` + `coachIntensity` + `coachLanguage` to the very top of `customer-settings.js` modal header, not buried in the Onboarding section. Currently requires scrolling past Status to see what personality was assigned. | S | M |
| 4 | Skip-to-step onboarding override | Add a dropdown in customer-settings Onboarding section: "Jump to question" (goal / age / gender / style / intensity / language). Write the new `onboardingState` to Firebase directly, bypassing the linear flow for customers who already answered some questions verbally. | M | M |
| 5 | Add customer from CSV | Add a `<input type="file" accept=".csv">` to the Add Customer modal that parses `name,phone` rows and runs `addCustomer()` for each, with a progress counter. | M | L |
| 6 | Duplicate phone guard | `add-customer.js:174` calls `addCustomer()` silently if the phone already exists. Show a yellow inline warning "Already in your list" before submission rather than a silent no-op. | S | M |
| 7 | First-session prompt for never-active customers | 24 hours after onboarding completes, if `daysSinceLastSession === 999` (never trained), fire a special "first session" draft from the worker instead of the standard morning check-in. Requires a new branch in `processCustomer` in `worker.js`. | M | L |
| 8 | Intro message A/B field | Add an `introMessageB` field in Tune AI → Templates and a "50/50 split" toggle. `add-customer.js` randomly picks A or B and logs the variant in `logActivity` as `intro-sent-A` or `intro-sent-B`. | M | M |
| 9 | Re-trigger onboarding from customer-settings | Add an "Re-trigger onboarding" button in `customer-settings.js` Onboarding section (alongside the existing Reset) that calls `/onboarding/begin` via the worker URL from `loadSettings()`. Currently only works via the Add Customer flow. | S | M |
| 10 | Customer name live-update in sidebar | When a name is changed in customer-settings and saved, dispatch a custom event that `customers.js` listens for and re-renders that specific sidebar row — currently requires a full page refresh. | S | M |

### 2. Conversation UX
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 11 | Draft diff view | When AI overwrites the textarea, show a diff banner (strikethrough old text / highlighted new text) with "Accept AI" / "Keep mine" buttons rather than silently replacing. Store previous value in `chat.js` as `previousDraftValue`. | M | L |
| 12 | Character count + WhatsApp limit warning | Show a live char count below the `chat-input` textarea in `chat.js`. Flash yellow at 1500 chars and red at 2000 (WhatsApp soft cap). | S | M |
| 13 | Draft history undo queue | Store the last 3 AI drafts per chat session in a module-level array in `chat.js`. Add an "Undo" button next to the composer that restores the previous draft when accidentally discarded. | S | L |
| 14 | "Send now" on pending-draft banner | Add a "Send now" button directly on the `pending-draft-banner` in `chat.js:358` — same behavior as clicking the textarea + Send, but in one tap. Currently requires reading the draft, finding the send button, and clicking it. | S | L |
| 15 | Reply-to threading | The Periskope send API already accepts `reply_to` (`periskope.js:38`) but no UI exposes it. Add a "↩ Reply" swipe-action on inbound bubbles that sets a `replyToMessageId` state variable consumed by `onSend`. | M | M |
| 16 | Unread count badge on sidebar row | When the dashboard is backgrounded, Periskope polling still runs. Increment a per-customer `unreadCount` when a new inbound arrives while that customer's chat isn't open; show the count as a red badge on the sidebar row. | M | M |
| 17 | Full-timestamp tooltip on bubbles | `formatTime` in `chat.js:1059` shows "HH:MM" for same-day messages. Add a `title` attribute to `.bubble-meta` with the full IST datetime so operators can hover for exact time without opening the inspector. | S | S |
| 18 | Auto-focus textarea on customer select | After `renderShell` in `chat.js:260`, call `document.getElementById('chat-input').focus()` so operators can start typing immediately. | S | S |
| 19 | Send confirmation toast | Replace the silent 800ms poll refresh after `onSend` (`chat.js:963`) with a 2-second "Sent ✓" toast overlay on the messages pane. | S | M |
| 20 | "Not now" snooze on pending draft | Add a "Snooze 1h" button on the `pending-draft-banner` that calls `clearPendingDraft` + `addScheduledReminder` with `fireAt = Date.now() + 3600000` and `reason: 'snoozed pending draft'`. | S | M |
| 21 | Inline message search | `Ctrl+F` in the chat pane opens an inline search bar that client-side-filters `currentMessages` by body text and scrolls to the first match. | M | M |
| 22 | Attachment inline thumbnail | `mediaLabel` in `chat.js:917` returns `[image]` etc. Extend to render a small `<img>` thumbnail for image-type media using the Periskope message URL if available in the payload. | L | M |

### 3. Multi-customer ops
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 23 | Customer search bar | Add a `<input type="search" id="cust-search">` above `.customer-list` in `index.html` that client-side-filters `allCustomers` (`customers.js:56`) by name or phone on `input` events. | S | L |
| 24 | Sort by dormancy | Add a sort dropdown above the customer list: "Sort: as added / most dormant / lowest habit score." Re-orders `allCustomers` array by `daysSinceLastSession` descending and re-calls `renderList`. | S | L |
| 25 | Sort by habit score (ascending) | Second sort mode ordering by `habitScore` ascending — lowest habit score = highest risk. | S | M |
| 26 | "Needs attention" smart filter tab | Add a 🔴 tab button above the list that filters to customers where `daysSinceLastSession > 5 && aiMode === 'off'` — falling behind with no automation running. | M | L |
| 27 | Pending drafts overview tab | Add a 📝 tab that lists all customers where `hasDraft === true` (already fetched in `customers.js:52`). Clicking a row opens that customer's chat so operators can batch-approve pending drafts. | M | L |
| 28 | Bulk AI mode set | Add checkboxes to sidebar rows and a "Set AI mode for selected" dropdown. Calls `writeConfig(phone, { autoCoachMode: value })` for all checked customers via `Promise.all`. | M | M |
| 29 | Bulk pause/unpause | Same checkbox UI — "Pause selected / Unpause selected" action that writes `paused: true/false` to Firebase for all checked phones. | M | M |
| 30 | Customer count header | Add a mini stats row above the customer list: "N customers · N active today · N pending drafts" using the already-available `allCustomers` array. Zero Firebase reads needed. | S | M |
| 31 | Label / tag system | Add a `tags: string[]` field to Firebase config per customer. Render colored dot tags on sidebar rows. Add a "Filter by tag" dropdown above the list. | M | M |
| 32 | Last inbound preview in sidebar | Fetch the last `from_me: false` message body for each customer from Periskope (or from the Firebase `activity` log) and display a truncated preview in the sidebar row beneath the habit score. | M | M |

### 4. Automation & scheduling
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 33 | "Next 3 send times" preview | Below the `sendTimeIST` picker in `customer-settings.js`, add a helper that runs `computeNextMorningCron` logic from `chat.js:575` and shows the next 3 firing times in IST so operators can verify the schedule. | S | M |
| 34 | Skip tomorrow's check-in | Add a "Skip tomorrow" button in the `next-up-banner`. Writes `skipNextCron: true` to Firebase config. Worker's `runCron` loop in `worker.js:1007` checks this flag and clears it after skipping. | S | M |
| 35 | Reminder recurrence option | Add `repeatEvery: 'daily' | 'weekly' | null` to `addScheduledReminder` in `firebase-db.js:240`. Worker's `scanAndFireReminders` re-seeds a clone with the next `fireAt` after firing a recurring reminder. | L | L |
| 36 | Edit reminder fire-time | Add an inline "Edit time" button on pending reminder rows in `customer-settings.js:376` that renders a `<input type="datetime-local">` in-place and calls `update(reminderRef, { fireAt: newTime })`. | M | M |
| 37 | Automation feed filter | Add "Filter by customer phone" and "Filter by type (cron/webhook)" selects to the `auto-feed` panel in `automation-feed.js:22`, which currently shows the last 25 global events with no filter. | M | M |
| 38 | Post-workout window configurable | Worker hardcodes a 4-hour post-workout window in `seedPostWorkoutReminders`. Expose it as a `postWorkoutWindowHours` field in Tune AI → Safety (alongside the existing safety fields in `worker.js:29`). | S | M |
| 39 | "Remind when they next message" trigger | New reminder type: `triggerOnNextInbound: true` stored in config. When the webhook fires for that customer, create a pending draft instead of replying — even if AI mode is off. Clears after one use. | M | L |
| 40 | Reminder reason autocomplete | Add a `<datalist id="rp-reason-list">` to `chat.js:308` with common options: "check if they trained", "follow up on shoulder", "end of week review". Attached to the existing `rp-reason` input. | S | S |
| 41 | Daily operator digest in Firebase | After each cron run, write a summary to `digest/YYYY-MM-DD`: messages sent, pending, discarded, reminders fired. The sidebar renders a collapsible "Today: N sent" strip using this. | M | M |
| 42 | Bulk cohort schedule | "Schedule for all gym customers" action that seeds a `manual` reminder at a specified datetime for all customers where `autoDetectedType === 'gym'`. Done as a batch Firebase push. | M | M |

### 5. AI quality control
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 43 | 👎 ratings review panel | Add a "Rated drafts" tab in Tune AI that queries the last 20 `aiRatings` entries where `rating === 'down'`, displays draft + reason, with a "Add as anti-example to prompt" one-click paste. | M | L |
| 44 | Prompt character counter | Show a live char count on each system prompt textarea in Tune AI → Prompts. Warn in yellow at 1500 chars, red at 2500 (per "Don't over-control" in `tune-ai.js:473`). | S | M |
| 45 | A/B sandbox compare | In Tune AI → Sandbox, add a "Run A/B" button that calls `generateMessage` twice — once with the current saved prompt and once with the prompt currently in the textarea — and renders both outputs side-by-side. | M | M |
| 46 | Prompt change log with rollback | On `saveGlobalConfig` in `global-config.js:22`, snapshot the previous prompts to `globalConfig/history/{timestamp}`. Add a "Restore from history" dropdown in Tune AI → Prompts. | M | L |
| 47 | Sandbox multi-customer batch run | Add a "Run on 5 random customers" mode in the Sandbox that loops `runSandbox` across 5 randomly-selected phones and renders all outputs in a scrollable list for a quick sanity check after prompt edits. | M | M |
| 48 | Approval-rate metric in Usage | Track `approved-draft-sent` vs `draft-discarded` from the `activity` log (both are already written in `chat.js`). Show weekly approval rate % in Tune AI → Usage tab alongside the token cost table. | M | L |
| 49 | Hold rate metric | Same pattern: `draft-held` events vs total auto-sends. High hold rate signals operator distrust of auto-send. Show as a percentage in the Usage tab. | M | M |
| 50 | AI error rate alert banner | In Tune AI → Usage, scan `aiUsage` for days where `errors/calls > 0.1`. If any day in the last 7 days exceeds 10% error rate, show a red banner at the top of the Usage pane. | S | M |
| 51 | 9-personality side-by-side compare | In Tune AI → Personalities, add a "Compare all" button that runs the Sandbox on the same customer + inbound across all 9 personalities and renders a scrollable grid with one output per column. | L | L |
| 52 | Sandbox save-to-test-set | Add a "Save as test case" button in the Sandbox that writes `{phone, inbound, expectedToneKeywords: ''}` to `globalConfig/evalSet` in Firebase for later regression use. | M | M |

### 6. Workflow & productivity
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 53 | Keyboard nav between customers | Add `Ctrl+↓` / `Ctrl+↑` listeners in `app.js` that find the currently-active `.cust-row` and call `onSelect` on the next/previous sibling. | S | M |
| 54 | Pinned notes banner above chat | The `notes` field in customer config is only visible inside the settings modal. Render a sticky yellow banner between the chat header and messages when `notes` is non-empty. | S | M |
| 55 | Recent customers shortlist | Track the last 5 opened phone numbers in `localStorage` with timestamps. Show a "Recent" section at the top of the sidebar above the main list. | S | M |
| 56 | Inline rename customer | Double-click the name in `.chat-header-name` to toggle it into an editable `<input>` that calls `saveSettings` on blur. Currently requires opening the settings modal. | S | M |
| 57 | Copy draft to clipboard | Add a "📋 Copy" icon button next to the `chat-input` textarea that copies the current text to the clipboard without sending. | S | S |
| 58 | Global Tune AI keyboard shortcut | Register `Ctrl+Shift+T` in `app.js` to call `openTuneAi()` from anywhere, analogous to the existing `Ctrl+J` for coach draft. | S | S |
| 59 | "Compose for all due customers" batch mode | A "Batch compose" view that shows all customers whose `next-up` cron time is within 2 hours, and lets the operator pre-draft messages for each in sequence before any are sent. | L | L |
| 60 | Multi-tab sync via BroadcastChannel | When two browser tabs have the dashboard open, a `BroadcastChannel` message keeps the selected customer and pending draft state in sync so tabs don't fight over the same Firebase writes. | M | S |
| 61 | Undo send (recall window) | After a manual send, show a 5-second "Undo" toast. If clicked, call the Periskope delete-message API (if available). Currently, once sent, there's no recall path. | M | M |
| 62 | Pre-canned message templates | Add a "Templates" section in customer-settings where operators store 3-5 pre-written messages for common scenarios (e.g., "week summary", "encouraging push"). A dropdown in the composer pastes the selected template into the input. | M | M |

### 7. Analytics & reporting
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 63 | Weekly engagement summary view | Add a "Reports" modal (accessible from the sidebar header) showing messages sent per day for the last 14 days, broken down by cron / webhook / manual. Reads from each customer's `activity` log. | L | L |
| 64 | Response rate per customer | Compute `inbound messages received / outbound messages sent` per week per customer. Surface as "Response rate: X%" in the customer-settings Status section. | M | L |
| 65 | Draft quality trend chart | Plot the rolling 30-day 👍/👎 ratio from `aiRatings` in Tune AI → Usage as a simple sparkline. A rising 👎 rate signals prompt drift. | M | M |
| 66 | Dormancy leaderboard | A "Who hasn't trained in >7 days?" sorted list in the Reports section. Uses the already-available `daysSinceLastSession` from `customers.js:68` — zero new Firebase reads. | S | M |
| 67 | Cost per customer breakdown | Break down `aiUsage` token costs by customer phone key. Worker already logs per-customer `activity` including AI calls. Show in Tune AI → Usage as a table: phone / messages / cost. | M | M |
| 68 | Customer tier distribution summary | Add a card in Tune AI → Usage showing how many of the loaded customers are in each Ferra tier (Tier 1–5) based on the `tierLabel` field from the workout export. Zero Firebase reads. | S | M |
| 69 | First-message-to-first-workout funnel | Track: did the customer record their first workout within 7 days of `conversationStartTs`? Store `firstWorkoutAfterOnboarding: boolean` in Firebase config when detected. Report funnel rate in Reports. | L | L |
| 70 | Message volume heatmap | Show a day-of-week × hour grid in Reports showing when outbound AI messages are being sent. Helps identify if scheduling aligns with customer responsiveness. | M | M |
| 71 | Personality performance comparison | Group customers by `coachPersonality` (stored in Firebase config post-onboarding), compute average `habitScore` per group. Surfaces which personality style correlates with best outcomes. | L | L |
| 72 | Monthly report download | A "Download summary" button in Reports that generates a plain-text monthly summary (N customers, N messages, cost, approval rate) and triggers a browser `<a download>`. | M | M |

### 8. Safety, compliance, observability
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 73 | Webhook failure rate alert | Scan the last 100 entries of the global `automation/feed` on load. If `result.error` count / total > 20% in the last 24 hours, show a red alert banner in the sidebar. | M | M |
| 74 | Activity log pagination | The `subscribeActivity` call in `customer-settings.js:181` fetches 20 events. Add a "Load 20 more" button that calls `subscribeActivity(phone, n + 20, ...)` and appends to the existing list. | S | M |
| 75 | Per-customer audit export | Add a "Download activity log" button in customer-settings that formats all fetched `activity` events as CSV (`ts,direction,source,action,message`) and triggers a browser download. | S | M |
| 76 | Worker health check indicator | In `automation-feed.js`, add a periodic `fetch(workerUrl + '/health')` every 5 minutes. Show a "Worker: ✓" or "Worker: ✗" badge at the top of the sidebar next to the version label. | S | M |
| 77 | Hindi opt-out keyword expansion | Extend `OPT_OUT_KEYWORDS` in `worker.js:41` with "galat number", "bhool jao", "nahi chahiye", "chodo". Currently the Hindi list has "mat karo / mat bhejo / band karo" but misses these common variants. | S | M |
| 78 | Rate limit state per customer in sidebar | Show a small colored ring on each sidebar row: green = can send now, yellow = min-gap active, red = daily cap hit. Derived from `lastOutboundAt` + `outboundCountToday` already in allCustomers. | M | L |
| 79 | Quiet-hours override per customer | Add a `quietHoursOverride: boolean` field to customer config (in `customer-settings.js`). Worker checks it before applying the global quiet-hours gate. For VIP customers who want late-evening responses. | S | M |
| 80 | Message send retry (once) | When `sendViaPeriskope` fails in `worker.js`, currently the customer gets no message. Wrap the send in a retry with 2-second backoff before writing a `send-failed` activity entry. | S | L |
| 81 | Operator action audit log | When prompts are changed in Tune AI, write `{ts, action: 'prompt-updated', field: 'coach', charsBefore, charsAfter}` to `globalConfig/changeLog` in Firebase. Visible in a read-only "History" list in Tune AI. | M | M |
| 82 | Duplicate webhook counter | When `result.ignored === 'duplicate'` in `worker.js:369`, skip the feed entry (as today) but also increment a `workers/duplicateCount/YYYY-MM-DD` counter in Firebase. Show this in Tune AI → Usage to track Periskope re-delivery rate. | S | M |

### 9. Integrations
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 83 | Webhook setup one-click button | Worker has `/periskope/webhook-setup` endpoint (`worker.js:734`). Surface it as a "Set up Periskope webhook" button in the Settings modal that POSTs to this endpoint and shows the result inline. Currently requires a manual curl. | S | L |
| 84 | WhatsApp deep link in chat header | Add a `wa.me/+{phone}` anchor in `.chat-header-info` so operators can jump to the native WhatsApp thread on mobile by clicking the phone number. | S | M |
| 85 | Firebase connection status badge | Subscribe to Firebase's `.info/connected` real-time path and show a "Firebase: ● connected / ⚠ reconnecting" badge next to the version number. | S | M |
| 86 | Calendar export of reminders | In customer-settings, add an "Export reminders as .ics" button that generates an iCalendar file of all pending reminders for the customer, downloadable for Google Calendar / Apple Calendar import. | M | M |
| 87 | Zapier/webhook notification for pending drafts | When a pending draft lands in Firebase for a customer, optionally POST to a configurable `notifyWebhookUrl` stored in settings. Enables Slack/email notifications without building a native push layer. | M | M |
| 88 | Customer list CSV export | Add "Export as CSV" in the sidebar header that downloads: phone, name, habit score, tier, days since last session, AI mode, pending draft (yes/no). Uses already-loaded `allCustomers` array. | S | M |
| 89 | Workout data freshness toast alert | The workout banner (`workout-banner.js:36`) already colors the dot red if data > 6h old. Add a one-time toast alert the first time it goes stale in a session rather than relying on the operator to notice the dot. | S | M |
| 90 | Multi-Periskope account switcher | Store an array of `{label, workerUrl}` pairs in `localStorage` instead of a single `workerUrl`. A dropdown in the Settings header lets operators switch between Ferra numbers. | L | M |
| 91 | Ferra export direct open | Add an "Open Ferra export" icon link in the workout banner that opens the raw export URL in a new tab for quick inspection when troubleshooting data gaps. | S | S |
| 92 | Periskope chat search passthrough | Add a search input in the chat pane that calls the Periskope messages endpoint with a `q` parameter (if Periskope supports full-text search), rendering matching messages without full message history pagination. | M | M |

### 10. Performance & scale
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 93 | Virtual scroll for large customer lists | `customers.js:93` renders all rows as a full HTML string. For 100+ customers, replace with a virtual scroll that renders only the ~15 visible rows using CSS `position: absolute` + scroll event listener. | L | M |
| 94 | Paginate Periskope message history | `refreshMessages` in `chat.js:649` fetches 100 messages at once. Add a "Load older messages" button that fetches the previous 100 (incrementing `offset`) and prepends them to the messages container. | M | M |
| 95 | Auto-expire workout data cache | `workout.js:6` uses a module-level `cache` that only refreshes on force-click. Add a `maxAgeMs: 5 * 60 * 1000` check at the top of `loadWorkoutData` so the cache self-refreshes without operator action. | S | M |
| 96 | Reduce webhook feed query size | `subscribeWebhookEventsForChat` in `firebase-db.js:268` queries `limitToLast(300)`. For high-volume customers, this can be slow on open. Default to `limitToLast(50)` and add a "Load more history" button that increases it. | S | M |
| 97 | Pause polling when tab backgrounded | `chat.js:1041` polls every 20s unconditionally. Add `document.addEventListener('visibilitychange', ...)` to pause polling when `document.visibilityState === 'hidden'` and resume on visible. | S | M |
| 98 | Lazy-init Tune AI modal | `buildModalHtml()` in `tune-ai.js:237` runs at `DOMContentLoaded`. Defer it to the first click of `#open-tune-ai` to remove ~200ms from initial page load. | S | S |
| 99 | Single Firebase read for customer list | `customers.js:44` runs one `readConfig` + one `readPendingDraft` per customer in parallel. Replace with a single `get(ref(db, 'customers/'))` tree read and parse all configs client-side — one round trip instead of 2N. | M | M |
| 100 | Worker KV cache for global config | `getGlobalConfig()` in `worker.js` fetches from Firebase on every webhook invocation. Cache it in Cloudflare KV with a 60-second TTL to eliminate one Firebase round-trip per webhook call. | M | M |
| 101 | Cloudflare KV cache for Ferra export | Worker already uses `cf: { cacheTtl: 300 }` for the Ferra export HTTP cache. Add an explicit KV write/read as a fallback for stale-while-revalidate behavior when Cloudflare's CDN cache misses cold. | M | M |
| 102 | IndexedDB offline cache for workout data | Store the last successful workout export in `IndexedDB` (via a simple wrapper) so the dashboard can render customer rows in read-only mode when the worker is temporarily down. | L | M |

### 11. Mobile & responsive design
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 103 | Responsive sidebar collapse | On screens < 768px, add a hamburger button that slides the sidebar off-screen (CSS `transform: translateX(-100%)` transition), showing only the chat pane. | M | L |
| 104 | Bottom composer on mobile | On narrow screens, ensure the `.chat-composer` stays above the iOS/Android soft keyboard using `env(safe-area-inset-bottom)` CSS padding and `-webkit-fill-available` for the messages pane height. | M | M |
| 105 | PWA manifest + install prompt | Add `manifest.json` with app name, icons, and `display: standalone`, plus a basic service worker for the install-to-home-screen prompt on mobile browsers. | M | M |
| 106 | Touch-friendly pill tap targets | The A/D/⏸ status pills in `customers.js:100` are < 24px touch targets. On mobile breakpoint, increase to at least 44×44px via CSS min-height/min-width. | S | M |
| 107 | Swipe-to-approve draft | On touch devices, add a horizontal swipe-right gesture on the `pending-draft-banner` that triggers the same action as "Send now". | M | M |
| 108 | Sticky chat header | Ensure `.chat-header` has `position: sticky; top: 0; z-index: 10; background: var(--bg)` so it remains visible when scrolling through long message histories on mobile. | S | M |
| 109 | Landscape mode layout | On tablets in landscape, render the sidebar and chat pane side-by-side at 30/70 split (already the desktop layout) rather than stacked. Add a landscape media query. | S | M |
| 110 | Disable pinch-zoom on chat pane | Add `minimum-scale=1` to the viewport meta tag in `index.html:5` to prevent accidental pinch-zoom on the message bubbles breaking the layout. | S | S |

### 12. Accessibility & internationalization
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 111 | ARIA roles on all modals | Add `role="dialog"`, `aria-modal="true"`, `aria-labelledby` to the Tune AI, Add Customer, and AI Inspector modals. The Settings modal already has `role="dialog"` (`index.html:38`) — extend the pattern. | S | M |
| 112 | Focus trap in modals | When a modal opens, trap `Tab` key cycling within it. When it closes, return focus to the trigger button. Currently Tab escapes to background content. | S | M |
| 113 | Screen reader draft announcement | When an AI draft loads into `chat-input`, insert/update an `aria-live="polite"` region announcing "AI draft ready" so screen reader users don't miss the load. | S | M |
| 114 | Keyboard-accessible bubble hide button | `.bubble-hide` renders on CSS hover (`:hover`). Add `opacity: 1` when the bubble is focused via keyboard so the button is reachable without a mouse. | S | M |
| 115 | Devanagari font fallback | Add `'Noto Sans Devanagari', 'Kohinoor Devanagari'` to the CSS `font-family` stack in `styles.css` so Hindi-script customer messages render cleanly rather than falling back to the system font. | S | M |
| 116 | Color contrast audit on sub-text | `.cust-sub`, `.bubble-meta`, and `.auto-time` use light gray on white backgrounds. Run a WCAG AA audit and bump any pair below 4.5:1 contrast ratio. | S | M |
| 117 | Reduced-motion CSS block | Add `@media (prefers-reduced-motion: reduce)` in `styles.css` to disable the hold-banner pulse animation and the `.busy` spinner on draft buttons. | S | S |
| 118 | `dir="auto"` on bubble text | Add `dir="auto"` to `.bubble-text` span so RTL scripts (Urdu, Arabic customer names) auto-align correctly without manual override. | S | S |

---

## Prompt Engineering (count: 99)

### 1. Persona consistency
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 1 | Founder-framing anchor line | `DEFAULT_SYSTEM_COACH` introduces "Rohit, founder of Ferra" but doesn't say WHY Rohit personally messages customers. Add: "You personally message each customer — they know the founder is checking in on them. This human weight matters; don't sound like a bot." (`defaults.js:1`) | S | M |
| 2 | "Don't start with I" hard rule | Add to all three base prompts: "Never open with 'I' — start with a fact, the customer's name, or the topic directly. 'Three days since your last session' not 'I noticed you haven't trained.'" | S | L |
| 3 | No "just checking in" list | Add to all base prompts: "Never open with: 'Just checking in', 'Wanted to follow up', 'Hope all is well', 'Reaching out because'. Open with a specific observation or question." | S | L |
| 4 | Sign-off style per personality | Each personality in `DEFAULT_PERSONALITIES` (`defaults.js:111`) should specify its sign-off convention. High-Energy: ends with a forward-looking call-to-action. The Pro: ends flat, no trailing question. Friend: sometimes ends with banter. Add `Sign-off:` subsection. | S | M |
| 5 | Tone calibration by habit score | Add to `DEFAULT_SYSTEM_COACH`: "If habit score < 30, lead with empathy before any push — they're struggling. If habit score > 70, skip the warm-up and go straight to challenge." (`defaults.js:1`) | S | M |
| 6 | Male vs female persona voice cue | `interpolatePersonality` in `worker.js:714` swaps `{{personaName}}` (Rohit/Ashima). Add a `{{personaContext}}` placeholder: for Rohit "founder of Ferra who built the machine", for Ashima "head coach at Ferra who works directly with members". | S | M |
| 7 | No emoji creep clarification | Current rule is "no emojis unless they used them first." Add example: "If they sent 'ok 👍' — that counts. If they sent 'yes' — it doesn't. Match the actual register, not the mood." | S | M |
| 8 | No ALL CAPS creep in non-High-Energy | `DEFAULT_PERSONALITY_HIGH_ENERGY` allows occasional ALL CAPS. Add to `COMMON_TAIL` in `defaults.js:98`: "ALL CAPS allowed ONLY in High-Energy personality. All other personalities: no caps emphasis, ever." | S | M |

### 2. Context engineering
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 9 | Inject customer notes field | The `notes` field is stored in Firebase config (`customer-settings.js:6`) but is NEVER passed to `buildFerraPrompt` or `buildGymPrompt` in `prompt.js`. Add an "Operator notes (visible only to you):" block in both prompt builders. One-line change per function. | S | L |
| 10 | Exercise names in 14-day block | `buildFerraPrompt` in `prompt.js:86` shows "5 exercises, 22 min" but not what those exercises are. When the workout export includes exercise names in `dailyActivity`, inject them: "Exercises: Chest Press, Row, Shoulder Press, Squat." | M | L |
| 11 | Progressive overload definition | `progressiveOverloadCount` appears in the prompt context (`prompt.js:90`) but isn't defined. Add: "Note: progressive overload = customer beat a previous personal record (heavier weight or more reps) on that exercise. If non-zero, it's a big win — always acknowledge it." | S | L |
| 12 | Segment label explanation | The prompt includes `Segment: dormant` etc. Add a key: "Segment meanings: active = trained in last 7d; at-risk = 8–14d; dormant = 15+ days; new = <5 sessions ever." | S | M |
| 13 | Month-over-month training comparison | `getRecentDailyActivity` covers 14 days. Add a "This month vs last month" comparison line using `userMonthlySummaries` in `workout.js:86`. Format: "This month: 8 sessions. Last month: 12 sessions." | M | M |
| 14 | Time-since-last-message injection | Compute `Date.now() - parseTimestamp(last20.slice(-1)[0]?.timestamp)` and inject "Time since last message from customer: Xh" before the "Recent messages" block. Tells the model whether this is a hot reply or a cold restart. | S | L |
| 15 | "No prior context" explicit cap | After the last-20-messages block add: "IMPORTANT: You are only shown the above. You have NO knowledge of any conversation before these messages, regardless of how long the customer has been with Ferra." | S | M |
| 16 | Weekly goal progress for Ferra customers | For Ferra customers, derive a weekly session count from `getRecentDailyActivity` for the current Monday–today and inject: "This week (Mon–today): 2 sessions." Provides the equivalent of the gym prompt's weekly goal context. | M | M |
| 17 | Streak fragility flag | When `streak.days <= 3 && streak.active`, inject: "Streak is fragile (N days). Don't mention the number as an achievement yet — just keep them going." When `streak.days >= 7`, inject "Streak is meaningful — mention it." | S | M |

### 3. Anti-hallucination & grounding
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 18 | Move anti-hallucination rules to top | In `DEFAULT_SYSTEM_COACH`, `DEFAULT_SYSTEM_REPLY`, and `DEFAULT_SYSTEM_GYM_COACH` (`defaults.js:1`), the anti-hallucination block is buried after the behavioral instructions. Move it to the very first section — model attention is highest at the top. | S | L |
| 19 | Grounding checklist instruction | Add: "Before mentioning any fact (session date, streak, score, exercise), verify it is explicitly stated in the context above. If you cannot point to the exact line, do not say it." | S | L |
| 20 | Forbidden vague praise list | Add to all prompts: "Never say 'your recent progress', 'you've been working hard lately', 'you've been consistent' unless you can cite a specific workout entry above that proves it." | S | M |
| 21 | No extrapolation from one session | "If you see one workout this week, do not say 'good week' or 'you're on track'. One session doesn't confirm a full week. State the fact: 'One session so far this week.'" | S | M |
| 22 | Zero-history customer guardrail | The zero-history branch in `buildFerraPrompt` (`prompt.js:106`) is thin. Add explicit: "Do NOT reference workouts they haven't done. Do NOT say 'your first session' as if it's imminent. Ask one open question about what's holding them back." | S | M |
| 23 | Injury hard-stop script | `COMMON_TAIL` in `defaults.js:98` has "No medical advice" but lacks a specific script. Add: "If the customer mentions pain or injury in the visible chat, respond ONLY with: 'Rest it and see a physio if it persists — don't push through pain.' No other content in that message." | S | L |
| 24 | Specific forbidden openers | "Never begin a message with: 'I noticed', 'I can see', 'I know you've been', 'Based on your history' — these sound fabricated and robotic. State observations as facts: '3 days since your last session' not 'I noticed you haven't trained in 3 days.'" | S | L |
| 25 | Schedule-assumption guardrail | "Never assume the customer's schedule, morning routine, work hours, or commute unless they explicitly stated it in the visible chat above. 'How does your week usually look?' is fine; 'I know mornings are tough for you' is not." | S | M |

### 4. Output format constraints
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 26 | Two-sentence max rule | Tighten "usually 1 to 3 short sentences" to: "Default to 2 sentences. Use 3 only if the customer asked a multi-part question requiring a full answer. Never write 4+ sentences in a single WhatsApp message." | S | L |
| 27 | No line-break / paragraph rule | Add: "Never insert line breaks or newlines in your output. WhatsApp renders them as paragraph breaks which looks formal and impersonal. Keep the message as one continuous text." | S | M |
| 28 | No rhetorical openers | "Never open with a standalone question as the first sentence. State an observation first, then ask. Example: 'Nothing logged yet today — did you train?' not 'Did you train today?'" | S | L |
| 29 | One call-to-action per message | "End with at most one question or request. If you have two things to ask, pick the one that matters more. Multiple questions in one WhatsApp message goes unanswered." | S | L |
| 30 | No filler acknowledgments | "Never open with 'Got it', 'Understood', 'Of course', 'Sure', 'Absolutely'. If the customer asked something, answer directly in the first clause." | S | L |
| 31 | Weekly goal "X of Y" format | For gym customers, always write the goal as "you're at 2 of 4 sessions this week" not "you've done half" or "50% complete." Number format is more actionable. Add to `DEFAULT_SYSTEM_GYM_COACH`. | S | M |
| 32 | Date reference as day name | "When referencing a specific workout date, use the day name ('Monday', 'yesterday') not the ISO date ('2025-05-12'). Customers don't think in ISO dates." | S | M |
| 33 | Length calibration by urgency | Add a length heuristic: "If the customer just reported a workout — 1 sentence acknowledgment, 1 sentence forward. If dormant >7 days — 2 sentences max, lead with warmth. If new customer first contact — 2-3 sentences is acceptable." | S | M |

### 5. Few-shot examples
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 34 | Three-scenario examples in Ferra coach prompt | Add a `## Examples` section to `DEFAULT_SYSTEM_COACH` with: (1) active customer with progressive overload — specific acknowledgment + forward ask; (2) dormant 10 days — gentle surface + open question; (3) new customer 0 sessions — curiosity opener about hesitation. | M | L |
| 35 | Anti-examples block (bad/good pairs) | Add to `DEFAULT_SYSTEM_COACH`: "Anti-examples (never do this): BAD: 'I know you were really busy this week.' GOOD: 'No sessions logged since Tuesday — what's the situation?' BAD: 'Great effort!' GOOD: 'Three sets of chest press and a row block — solid 22 minutes.'" | S | L |
| 36 | High-Energy personality example pair | The High-Energy prompt talks about "LOUD energy" but has no example. Add: "EXAMPLE — BAD: 'Let's go champion!' GOOD: 'Five sessions in 7 days — that's Tier 4 pace. KEEP THAT FIRE. Same time tomorrow?'" (`defaults.js:111`) | S | M |
| 37 | Analyst personality example | The Analyst prompt says "numbers first" but has no worked example. Add: "EXAMPLE: 'Habit score 73, up 11 from last week. Tier 4 is 2 sessions away — let's lock it in.'" (`defaults.js:184`) | S | M |
| 38 | Gym coach example for behind-goal | Add to `DEFAULT_SYSTEM_GYM_COACH`: "EXAMPLE — BAD: 'Did you work out today?' GOOD: 'Tuesday was your last logged session — you're at 2 of 4 for the week. Training today?'" | S | L |
| 39 | Post-workout ack example | The post-workout reminder uses the same coach prompt but has a distinct trigger. Add an example inline in the worker's prompt build for post-workout replies: "EXAMPLE: '25 min on the machine today — that compound block looked clean. Same tomorrow?'" | S | M |
| 40 | Hinglish worked example | Add one example of natural Hindi-English code-switch to the Ferra prompt: "HINGLISH EXAMPLE: 'Teen din ho gaye — aaj time hai kuch karne ka?' (not: 'Three din ho gaye. Kya aaj time hai?')" | S | M |

### 6. Customer-segment specialization
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 41 | Never-active (0 sessions) branch | In `buildFerraPrompt` (`prompt.js:59`), detect `daysSinceLastSession === 999` and inject a dedicated instruction: "This customer has NEVER trained on the machine. Do NOT reference workouts. Ask what's holding them back or offer to walk them through their first 10-minute session." | S | L |
| 42 | Dormant re-engagement tone (>14d) | When `daysSinceLastSession > 14`, inject: "This customer has been inactive for >2 weeks. Do NOT open with workout talk — start with a casual 'Hey, how've you been?' first message to re-establish connection. Workouts come second." | S | L |
| 43 | Tier 5 peer-register tone | When `tierLabel === 'Tier 5'`, inject: "This customer is in the top tier — an elite user. Speak peer-to-peer, not coach-to-student. Acknowledge the tier directly as a milestone." | S | M |
| 44 | Streak milestone celebration (10d, 21d, 30d) | Detect in `buildFerraPrompt`: if `streak.days >= 10 && streak.active`, add: "This customer has a ${streak.days}-day streak — acknowledge it as the opener: '${streak.days} days straight — that's real momentum. What's today's plan?'" Adjust for 21d and 30d milestones. | S | L |
| 45 | Post-onboarding warm welcome tone | When `onboardingCompletedAt` was within the last 24 hours, inject: "This customer just completed onboarding. This is their first 'real' check-in. Use an extra-warm, welcoming tone — they're fresh. Reference their chosen coach style naturally." | S | M |
| 46 | Gym customer badly behind goal | When gym customer has `thisWeek.length / weeklyGoal < 0.25` and it's Thursday or Friday, inject: "Customer is significantly behind their weekly goal with limited time. Don't blame — offer a 20-minute 'micro-session' as a partial win. 'Even 20 minutes counts — can you squeeze it in?'" | S | L |
| 47 | Cancelled/churned Ferra user | `workout.js:43` includes `cancelledUsers` in the index. In `buildFerraPrompt`, detect when a customer is in the cancelled cohort and inject: "This customer cancelled their Ferra subscription. Treat them like a gym customer — no machine data. Focus on re-engagement. Do NOT mention Ferra machine setup." | M | M |
| 48 | New customer first-week nurture | When `conversationStartTs` is within 7 days of now AND `daysSinceLastSession === 999`, inject: "This customer is in their first week and hasn't trained yet. Be patient and curious, not pushy. Ask about their schedule and what the machine looks like from their perspective." | S | M |

### 7. Conversation continuity
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 49 | Cold start vs mid-conversation age gate | Add: "If the last message in context is >72 hours old, open with a re-engagement opener (don't assume they remember the last thing). If <2 hours old, treat as mid-conversation and skip all pleasantries." (`prompt.js:119`) | S | L |
| 50 | Unanswered customer question detection | If the customer's last message in `last20` ends with '?', add: "The customer asked a question. Answer it FIRST before any workout pivot. Their question takes priority over your agenda." | S | M |
| 51 | Apology response protocol | When the customer's text includes "sorry", "I know I've been", "been busy", inject: "Customer is apologizing. Do NOT pile on — respond with 'No apology needed.' in the first clause, then pivot gently." | S | L |
| 52 | Workout report celebration protocol | When the last inbound includes "done", "finished", "completed", "just did", inject: "Customer just reported a workout. Celebrate FIRST (1 sentence), then ask about next session. Never skip the acknowledgment." | S | M |
| 53 | Stated-intent follow-through | If the previous outbound message ends with a question about a specific time ("What time today?"), and the customer didn't reply with a session report, inject: "You asked about a specific training time earlier. Reference that commitment — don't ignore it." | M | M |
| 54 | Topic persistence for named exercises | If any of the last 3 messages include a specific exercise name (e.g., "squat", "chest press"), inject: "Recent conversation mentions [exercise]. Continue that thread if relevant — don't pivot away mid-discussion of a specific movement." | S | M |

### 8. Time-of-day & day-of-week awareness
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 55 | Morning / afternoon / evening tone split | Add a time-of-day branch to `buildFerraPrompt` (`prompt.js:60`): IST < 12 → "It's morning — use fresh-start energy"; 12–18 → "It's afternoon — check-in tone, not morning energy"; 18–21 → "It's evening — use wind-down, reflect-on-today framing". | S | M |
| 56 | Monday fresh-start cue | Add: "If today is Monday, acknowledge the fresh week specifically: 'New week — clean slate.'" | S | M |
| 57 | Friday week-review cue | "If today is Friday, offer a brief week wrap-up: 'Almost weekend — where did we land on sessions this week?'" | S | M |
| 58 | Weekend tone modifier | "If today is Saturday or Sunday, don't demand workouts. Ask casually. Acknowledge that weekends have different rhythms — a nudge is fine, urgency is not." | S | M |
| 59 | Pre-holiday awareness | Inject a note when the IST date is within 2 days of a major Indian holiday (Diwali, Holi, Eid, Dussehra, Independence Day, Republic Day). "Note: major holiday approaching — lighter tone, no heavy accountability push." Requires a holiday date array in `prompt.js`. | M | M |
| 60 | Late-evening tone moderator | When IST hour is 20–21 (close to quiet hours at 21:00), inject: "It's late evening — keep the tone relaxed, no demanding calls to action. A gentle check-in only." | S | M |

### 9. Language & localization
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 61 | Hindi-first structure for Hindi-preference customers | Currently, `coachLanguage === 'Hindi'` is injected as `{{language}}` but the prompt template is English-first. Add: "For Hindi-preference customers: default to Hindi sentence structure, use English only for proper nouns and technical terms." | S | M |
| 62 | Hindi fitness vocabulary block | Add to the Ferra coach prompt: "Useful Hindi fitness terms: 'kal ka session' = yesterday's session, 'aaj ka target' = today's goal, 'adat' = habit, 'zyada mehnat' = harder effort, 'thoda aur' = a bit more." | S | L |
| 63 | Hinglish naturalness anti-example | Add: "BAD Hinglish (translation-y): 'Aapne aaj apna exercise kiya?' GOOD Hinglish (natural): 'Aaj kuch hua gym mein?' — test: would a young Mumbai professional say this to a friend?" | S | L |
| 64 | Honorific register mirroring | Detect from the last 3 customer messages whether they use formal "aap" or informal "tum". Inject: "Customer uses [formal/informal] register — match it. Don't mix registers mid-conversation." | M | M |
| 65 | English-only guardrail | "If the customer has written only English in the visible chat history and has no Hindi in their messages, stay fully in English. Do not introduce Hindi words unprompted." | S | M |
| 66 | Avoid romance-register Urdu | "Avoid Urdu-heavy words like 'meherbaan', 'janab', 'khidmat'. These read as overly formal or theatrical in a fitness coaching context. Stick to common Hindi-English fitness vocabulary." | S | S |

### 10. Workout-specific knowledge
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 67 | Exercise names in daily block | Change `buildFerraPrompt`'s 14-day block from "5 exercises, 22 min" to "Exercises: Chest Press ×3, Row ×4, Shoulder Press ×2 · 22 min" when exercise names are available in the workout export. The model can then reference specific movements. | M | L |
| 68 | Progressive overload callout rule | Add: "If `progressiveOverloadCount` is > 0 in today's or a recent session, ALWAYS acknowledge it by name: 'You pushed heavier today — that's the overload we want.' Never skip a progressive overload flag." | S | L |
| 69 | Rest day vs missed-day framing | "If the customer trained yesterday and has no session today, this may be a rest day — don't frame it as a 'missed' day. Only begin pushing if there are 2+ consecutive days with no session." | S | L |
| 70 | Short session acknowledgment | "A session under 15 minutes is a short/tight session — still counts. Don't call it insufficient. Say: 'Tight 12 minutes — showed up, that's the habit.'" | S | M |
| 71 | Habit score delta framing | "If habit score dropped >10 points week-over-week, do NOT mention the number directly (sounds like a report card). Instead: 'What changed this week?' If it rose >10 points, DO mention it: 'Score up 12 this week — trend is clean.'" | S | M |
| 72 | Tier advancement instruction | When a customer moves from Tier 3 → Tier 4 (requires storing `prevTierLabel` in Firebase config and checking on prompt build), inject: "This customer just moved up a tier. Open with that milestone: 'You're in Tier 4 now — that's the top 20%.'" | M | L |
| 73 | Session duration benchmarks | Add a reference frame: "Qualitative session lengths for this prompt — < 15 min: 'short, tight session'; 15–30 min: 'full session'; 30+ min: 'long session'. Use these qualitative terms, not just raw minutes." | S | M |
| 74 | Machine-specific first session script | For 0-session Ferra customers: "Suggest starting with the guided program — 3 exercises, ~12 minutes. Don't invent specific exercise names unless they appear in the workout data above." | S | L |

### 11. Failure mode handling
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 75 | Off-topic pivot template | Current instruction: "acknowledge in one line, then pivot." Replace with a concrete template: "Acknowledge + pivot formula: '[one-word topic ack], [pivot sentence]. Example: 'Sounds fun — did you get any training in this week?'" | S | M |
| 76 | Complaint / machine-issue script | "If the customer expresses frustration about the Ferra machine (it's broken, error, won't start), respond: 'Noted — I'll flag that for the team. Meanwhile, [next action for them].' NEVER promise a fix timeline." | S | L |
| 77 | Medical advice hard-stop | The injury guardrail is in `COMMON_TAIL` but lacks specificity. Add: "When the customer mentions pain/injury/discomfort, respond ONLY: 'Rest it and see a physio if it persists — don't push through pain.' One sentence. No additional coaching in that message." | S | L |
| 78 | Opt-out / "I want to cancel" draft | When the worker auto-pauses a customer (opt-out detected), the pending draft prompt should inject: "Customer indicated they want to stop. Acknowledge without pressure: 'Got it — pausing my messages. Ping me when you're ready to restart.' No other content." | S | M |
| 79 | Previous send-failed context | When the `activity` log shows the last action was `send-failed`, inject: "Note: previous message may not have reached this customer — do not reference it as if they read it." | M | M |
| 80 | Scheduling change request handling | "If the customer requests a different check-in time, respond acknowledging the time but note: 'I'll update your morning reminder to X.' Then the operator must manually change `sendTimeIST` in ⚙ — never auto-update times via AI." | S | S |

### 12. Eval methodology
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 81 | Define 10-case golden test set | Document in Tune AI → Best Practices a specific list of 10 canonical test cases: active Ferra customer / dormant 15d / new 0-session / gym mid-week / post-workout / post-onboarding / complaint / medical mention / off-topic chat / Hinglish. Before any prompt save, manually run all 10. | M | L |
| 82 | 👎 drain metric in Usage | Track the weekly ratio of 👎 ratings to total AI calls. If it exceeds 15%, show a warning in Tune AI → Usage: "Draft quality flag: 15%+ of drafts rated poor this week." | M | M |
| 83 | Pre-save eval checklist modal | When "Save all" is clicked in Tune AI, show a one-time confirmation modal: "Before saving, did you test on: □ active Ferra customer □ gym customer behind goal □ dormant customer □ new customer?" Operator must check boxes before save proceeds. | S | M |
| 84 | Personality-specific golden cases | For each of the 9 personalities, store one reference inbound + expected tone keywords in `globalConfig/evalSet` (e.g., High-Energy: expected output should contain at least one short exclamatory sentence). | M | M |
| 85 | Regression run on sandbox save | When "Save as test case" is clicked in the Sandbox (idea #52), also immediately re-run the current prompt against it and store the baseline output. On the next run, show a diff. | L | L |
| 86 | Weekly quality review reminder | Add a prompt in the Tune AI Best Practices tab: "Run the golden test set at least weekly. Every 👎 draft you intercept is a training signal — fold it into the prompt as an anti-example before the next weekly run." | S | S |

### 13. Prompt versioning & rollback
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 87 | Prompt history snapshot on save | On every `saveGlobalConfig` call in `global-config.js:22`, write the previous `prompts` + `personalities` snapshot to `globalConfig/history/{timestamp}`. No UI change needed — just the write hook. | S | L |
| 88 | Change note field before save | In Tune AI, add a "Reason for this change (optional):" text input that appears when any prompt textarea is modified. Its value is stored with the history snapshot. | S | M |
| 89 | Diff view for last change | A "What changed" button in Tune AI → Prompts that reads the last `globalConfig/history` entry and shows a line-by-line text diff against the current form values. | M | M |
| 90 | One-click rollback | "Restore previous" button in Tune AI that loads the most recent history snapshot into the form fields (without saving). The operator reviews and clicks "Save all" to confirm. | M | L |
| 91 | Auto-increment prompt version number | Add a `promptVersion: N` counter to `globalConfig` that increments on each save. Display next to the prompt label in Tune AI so operators can reference version numbers in conversations: "the v7 prompt". | S | S |
| 92 | Export current prompts as JSON | A "Download backup" button in Tune AI that calls `saveAs(JSON.stringify(currentPrompts), 'prompts-backup.json')`. Provides offline fallback against Firebase loss. | S | M |

### 14. Token efficiency
| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 93 | System prompt caching | Add `cache_control: { type: "ephemeral" }` to the system prompt block in `handleAnthropic` (`worker.js:307`) for Claude 3.5+ models. System prompts are identical across customers — caching them saves ~$0.01–0.05 per day at volume. | M | L |
| 94 | Compact workout block format | Switch `buildFerraPrompt`'s 14-day block from verbose multi-line to compact CSV: `2025-05-12: 5ex 22min +2po` (ex=exercises, min=minutes, po=progressive overload sets). ~30% token reduction on the context block. | S | M |
| 95 | Filter single-char message history | When building `last20` in `buildFerraPrompt` and `buildGymPrompt`, filter out messages where `body.trim().length < 3` (single-word acks: "k", "ok", "👍"). These add tokens with near-zero context value. | S | M |
| 96 | Haiku for reminder extraction | `extractAndScheduleReminders` in `worker.js` uses the main model. Switch to `claude-haiku-4-5-20251001` — the extraction task is simple pattern matching, not nuanced language. Haiku already used for workout extraction and onboarding parsing. | S | M |
| 97 | Module-level global config cache in worker | `getGlobalConfig()` is called multiple times per webhook invocation (onboarding, resolve system prompt, safety). Add a module-level `let globalConfigCache = null; let globalConfigFetchedAt = 0;` with a 30-second TTL inside `worker.js`. | S | M |
| 98 | Skip empty habit score block | In `buildFerraPrompt`, skip injecting the habit scores block entirely when `scores.length === 0` (dormant customers have nothing to show). Saves ~10–15 tokens per call. | S | S |
| 99 | Per-personality lazy-fetch | `getGlobalConfig` in the worker returns all 9 personalities on every call. Refactor to fetch only `global.personalities[config.coachPersonality]` for the current customer rather than deserializing the full object. | S | M |

---

## Quick wins shortlist (S effort + M+ impact)
1. **Customer search bar** (Dashboard #23) — `<input>` + client-side filter of `allCustomers`, single afternoon of work, highest daily operator pain
2. **Notes field injected into prompts** (PE #9) — one-line addition to `buildFerraPrompt` + `buildGymPrompt`; immediately improves drafts for every customer with notes
3. **"Send now" on pending-draft banner** (Dashboard #14) — a single button in `chat.js:358`; fixes the most friction-heavy step in the main operator loop
4. **"Don't start with I" + "No just checking in" anti-patterns** (PE #2, #3) — text-only prompt additions; fixes the two most common bad draft openers
5. **Anti-hallucination rules moved to top of system prompt** (PE #18) — cut and paste in `defaults.js`; model attention is highest at the top
6. **Sort by dormancy** (Dashboard #24) — 3 lines of array sort + re-render
7. **Draft history undo queue** (Dashboard #13) — store last 3 values in `chat.js` memory; prevents frustration when discarding good AI drafts
8. **"Needs attention" filter tab** (Dashboard #26) — client-side filter on `allCustomers`; makes high-risk customers impossible to miss
9. **Never-active (0 sessions) prompt branch** (PE #41) — inject different instruction in `buildFerraPrompt` when `daysSinceLastSession === 999`
10. **Pending drafts overview tab** (Dashboard #27) — tab that filters already-loaded `allCustomers` by `hasDraft === true`
11. **Progressive overload definition + callout rule** (PE #11, #68) — two sentence additions to `defaults.js`; acknowledges the most important workout signal
12. **Prompt history snapshot on save** (PE #87) — 5 lines added to `global-config.js:22`; prevents catastrophic one-click prompt loss
13. **Pinned notes banner above chat** (Dashboard #54) — renders the already-fetched `notes` field as a sticky bar; operators stop missing it
14. **Worker health check indicator** (Dashboard #76) — periodic `fetch('/health')` + badge in sidebar
15. **Cold start vs mid-conversation age gate** (PE #49) — one conditional injection in `buildFerraPrompt` based on last message timestamp age

---

## Ambitious bets (L effort but transformational)
1. **Weekly engagement analytics dashboard** (Dashboard #63 + #71) — full Reports view with per-customer response rates, personality performance comparison, and funnel from intro-to-first-workout. Requires aggregating Firebase activity logs across all customers.
2. **Prompt regression test runner** (PE #81 + #85) — golden test set in Firebase + automatic sandbox re-run on every prompt save with diff output. Converts prompt editing from guesswork to a software-like deploy-with-tests workflow.
3. **Reminder recurrence engine** (Dashboard #35) — `repeatEvery: 'daily'|'weekly'` in Firebase, with worker scanning for recurring reminders and cloning them. Enables set-and-forget weekly accountability beats per customer.
4. **"Batch compose" mode for due customers** (Dashboard #59) — surfaces all customers with imminent cron fires and lets operators pre-draft for each in a single focused session. Transforms the tool from "reactive per-customer" to "proactive batch coaching."
5. **Personality × habit-score performance study** (Dashboard #71) — join `coachPersonality` from Firebase config with `habitScore` trends from the workout export; compute which personality style correlates with best retention. Real-data A/B evidence for which coaching voice works.

---

## Top 10 to ship next
Rank-ordered by impact / effort / current pain:

1. **Customer search bar** (Dashboard #23) — unblocks scaling past 20 customers; zero risk
2. **Notes field injected into prompts** (PE #9) — immediately improves every draft for customers with contextual notes; touches two functions in `prompt.js`
3. **"Send now" on pending-draft banner** (Dashboard #14) — fixes the single most repeated friction in the operator loop; one button
4. **Anti-patterns: no "I" opener, no "just checking in"** (PE #2 + #3) — highest-leverage prompt text addition; fixes the most consistently bad draft openers with no downside
5. **Pending drafts overview tab** (Dashboard #27) — makes the "batch approval" workflow possible; uses data already loaded
6. **Sort by dormancy / "Needs attention" filter** (Dashboard #24 + #26) — operators can't manage at-risk customers they can't see; critical as list grows
7. **Progressive overload definition + callout** (PE #11 + #68) — Ferra's biggest differentiator vs a gym is the progressive overload data; it should be in every relevant draft
8. **Prompt history snapshot on save** (PE #87) — 5-line insurance policy against accidental prompt destruction; affects `global-config.js` only
9. **Draft history undo queue (last 3)** (Dashboard #13) — removes the fear of discarding a good draft by accident; purely client-side state in `chat.js`
10. **"Never-active customer" prompt branch** (PE #41) — the most underserved customer segment in the current prompt is someone who has never trained; a dedicated branch changes the entire first-session dynamic
