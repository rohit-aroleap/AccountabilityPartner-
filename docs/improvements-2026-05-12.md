# Improvement Ideas — 2026-05-12

Generated by routine: accountability-partner-improvement  
Codebase version: v1.033 · last commit: `65b4407 v1.033: bump dashboard event subscription cap + tighten replay window`  
Source docs read: What_is_Ferra page (403 — inaccessible), ferra.club (403 — inaccessible), full codebase (all 16 JS modules, worker/worker.js, README.md)  
Additional docs: Local Ferra files inaccessible from server environment — ideas grounded solely in codebase + product context inferred from code

---

## Summary
- Dashboard ideas: **96** across **12** categories
- Prompt engineering ideas: **98** across **14** categories
- Top emergent themes:
  1. **Context grounding** — customer `notes`, weekly summary lines, and stale-data signals are all missing from prompts despite being available in the codebase
  2. **Observability gaps** — ratings, replay events, and hold-rate data are logged to Firebase but never surfaced in the dashboard UI
  3. **Cost & token efficiency** — system prompt caching, Haiku routing, and context trimming are easy wins with meaningful savings at scale

---

## Dashboard (count: 96)

### Customer management & onboarding

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 1 | Onboarding checklist | After add-customer, show a mini progress bar in customer settings: ✅ Intro sent, ✅ AI mode set, ✅ Send time set, ✅ First workout seen. Currently there's no onboarding state beyond what's implicit in `js/add-customer.js`. | S | M |
| 2 | Bulk CSV import | Add a "Import from CSV" button next to the Add Customer button in `index.html`. Accepts columns: phone, name. Calls `storage.addCustomer()` + `firebase-db.writeConfig()` in sequence. Replaces manual one-at-a-time entry. | M | L |
| 3 | Customer tags | Add an array field `tags` in Firebase config (e.g., "shoulder-injury", "beginner", "VIP"). Render tag chips on `.cust-row` in `js/customers.js`. Filter customer list by tag from a dropdown. | M | M |
| 4 | Segment/type filter | Add a `<select>` above `.customer-list` to filter by segment (active / at-risk / dormant) or by type (Ferra / Gym). `js/customers.js:renderList` currently has no filtering capability. | S | M |
| 5 | Re-onboard action | Button in customer settings to re-send the intro message (picking the correct Ferra or Gym template). Useful when a customer went dormant and you want to restart the relationship. | S | M |
| 6 | Member-since date | Store and display a `joinedAt` timestamp when a customer is first added (`js/add-customer.js:onSave`). Show "Member since {date}" in the customer settings Status section. | S | S |
| 7 | Duplicate phone detection | In `js/add-customer.js:onSave`, check if the normalized phone already exists in `storage.parseCustomerPhones()` before saving. Show inline error: "This phone is already in your list." | S | M |
| 8 | E.164 inline validation | Validate the phone field in `add-customer.js` against `/^\+\d{8,15}$/` on blur. Show a red border + "Use international format: +91XXXXXXXXXX" hint. Currently validation is silent. | S | S |
| 9 | Customer archive | Add an "Archive" state to Firebase config (`archived: true`). Hide archived customers from the main list but preserve all data. Add a toggle in settings to show/hide archived customers. | M | M |
| 10 | Goal field | Add a free-text `goal` field in customer settings (e.g., "lose 10kg", "build strength before marathon"). Store in Firebase config. Inject into prompts via `js/prompt.js:buildDraftPrompt`. | S | L |
| 11 | WhatsApp name autofill | When typing a phone in add-customer, call Periskope contacts API to auto-fill the name field if the contact exists. Reduces manual entry errors. | M | S |
| 12 | Ferra machine model field | Per Ferra customer, store which machine variant they own (e.g., Home, Pro). Show in customer settings status. Reference in prompts for equipment-specific coaching. | S | S |

### Conversation UX

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 13 | Draft history (last 5) | Store the 5 most recent AI drafts per customer in Firebase (`/draftHistory`). Add arrow buttons on the pending draft banner to cycle through previous drafts before accepting. Currently discarding clears with no recovery. | M | M |
| 14 | Message templates quick-insert | A dropdown trigger button next to the 💪/💬 draft buttons in `js/chat.js:renderShell`. Shows 5-10 saved short templates (e.g., "Morning nudge", "Streak celebration"). One click → fills textarea. | M | M |
| 15 | AI badge inline expand | Instead of a modal (`js/chat.js:showInspectorModal`), clicking the AI badge opens an inline collapsible panel below the bubble. Faster review during message scanning. | M | M |
| 16 | Message delivery status | Show Periskope delivery status (sent / delivered / read) on outbound `.bubble.out`. Requires fetching the message status from the Periskope API or relying on webhook events. | M | L |
| 17 | Scroll-to-latest button | When `#chat-messages` scrolls up more than 200px, show a floating `↓` button that scrolls to bottom. Currently the user must scroll manually after the AI badge updates re-render. | S | M |
| 18 | Chat search | A search bar that filters the visible chat bubbles by keyword. DOM-only, no Firebase query needed. Useful for finding a specific message in long threads. | S | M |
| 19 | Media preview | Currently `js/chat.js:mediaLabel` returns `[image]` or `[audio]`. For image messages, show a thumbnail preview if Periskope provides a URL. For audio, show a compact play button. | L | M |
| 20 | Right-click context menu on bubbles | Right-click a bubble → "Copy text", "Use as draft context", "Log as workout" (gym customers). More discoverable than keyboard shortcuts. | M | M |
| 21 | Bulk draft review | "Review all drafts" button in the sidebar header when multiple customers have `hasDraft: true`. Steps through each pending draft one by one without switching customers manually. | M | L |
| 22 | Edit mode indicator on textarea | When an AI draft is loaded into `#chat-input`, show a subtle "AI draft — tap to edit" state on the textarea border. Currently it's visually identical to a manual message. | S | S |
| 23 | Composer undo (Ctrl+Z) | Textarea currently has no undo beyond browser default. Add a local `undoStack` in `js/chat.js` so Ctrl+Z restores the pre-draft text if the user accidentally overwrites it. | S | M |
| 24 | Sent-message count tooltip | On the `cust-meta` date badge in the customer row, show "Sent X messages today" as a tooltip on hover. Pull from `outboundCountToday` in the cached Firebase config. | S | S |

### Multi-customer ops

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 25 | Sort customer list | Add sort controls above `.customer-list`: by last active (most dormant first), by habit score (desc), by message time (most recent first). Currently order is fixed to the phone list order in storage. | S | L |
| 26 | Unread message indicator | Track `lastViewedAt` per customer in `localStorage`. Show a dot badge on `.cust-row` when a customer's last inbound timestamp > `lastViewedAt`. | M | L |
| 27 | Global name/notes search | A text input at the top of the sidebar that filters `.customer-list` rows by name or notes. Instant DOM filter, no Firebase needed. | S | M |
| 28 | "Needs attention" shortlist | A pinned section at the top of the sidebar listing customers with: unreviewed pending draft, held auto-send, or 5+ days dormant. Requires reading `hasDraft`, `holdQueue`, `daysSinceLastSession` from cached data. | M | XL |
| 29 | Customer count badge | Show "12 total · 3 dormant · 2 drafts" in the sidebar header. Computed from `allCustomers` array in `js/customers.js:refresh`. | S | M |
| 30 | Keyboard navigation | Arrow keys to move between customer rows; Enter to open. Add `keydown` handler to the document in `js/customers.js:initCustomers`. | S | M |
| 31 | Customer row hover card | Hover a customer row for 300ms → show a floating card with last 2 message previews + AI mode. Reduces need to click into the chat just to check status. | M | M |
| 32 | Last-message preview in row | Show the last message text snippet (≤ 50 chars) in the `.cust-sub` area of the row, like a real messaging app. Requires storing `lastMessagePreview` in Firebase or computing from cached Periskope data. | M | L |
| 33 | Pinned customers | A ⭐ button on the customer row that pins them to the top of the list regardless of sort order. Store pinned phones in `localStorage`. | S | M |
| 34 | Customer list export | "Export CSV" button that downloads all customers: name, phone, habitScore, tier, daysSinceLastSession, aiMode. Uses `allCustomers` in `js/customers.js`. | S | S |
| 35 | Drag-to-reorder | Drag-and-drop to reorder customer rows. Store custom order in `localStorage`. Fallback to default if not set. | M | S |
| 36 | Customer list grouping | Toggle to group customers by AI mode (auto-send / draft-only / off) or by segment. Insert group headers in `js/customers.js:renderList`. | M | M |

### Automation & scheduling

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 37 | Cron retry on send failure | In `worker/worker.js:processCustomer`, if the Periskope send fails, schedule a retry entry in Firebase with a 15-min delay. Currently the error is logged but not retried. | M | L |
| 38 | Second check-in time | Allow a per-customer `eveningTimeIST` field (e.g., 6PM). The cron processes both morning and evening windows if configured. Currently only one `sendTimeIST` per customer. | M | M |
| 39 | Reminder recurrence | In `js/chat.js:saveReminder`, add a "Repeat daily at this time" toggle. Writes a `recurringReminder` node in Firebase. The cron re-seeds it each day. | M | M |
| 40 | Blackout dates | Global config field `blackoutDates: ["2026-10-20"]`. Cron and webhook skip all automation on these dates. Configurable in Tune AI → Safety. | S | M |
| 41 | Skip cron if already trained | In `worker/worker.js:runCron`, skip the morning check-in if the workout export shows the customer already trained before the send window. Avoids redundant "are you going to train?" messages. | M | L |
| 42 | Reminder edit UI | In `js/customer-settings.js:renderReminders`, add an "Edit" button on pending reminders (alongside the existing Cancel) to change the `fireAt` time. Currently reminders are cancel-only. | S | M |
| 43 | "Send now" on pending reminder | A "Fire now" button on pending reminders in customer settings. Calls the worker `/cron/run` endpoint scoped to that customer and reminder. | S | M |
| 44 | Run cron button | "Run cron now" button in the Automation feed panel (calls existing `/cron/run` endpoint). Currently only accessible via direct URL. | S | M |
| 45 | Browser push on pending draft | When a `pendingDraft` appears in Firebase for the active customer tab, trigger a browser push notification (`Notification` API). Useful when the dashboard is open in background. | M | M |
| 46 | Pause-until date | Add a `pausedUntil` timestamp field in Firebase config. Customer auto-resumes when `Date.now() > pausedUntil`. The cron checks this before sending. | S | M |
| 47 | Hold window duration setting | Expose the hold countdown duration (currently hardcoded in `worker/worker.js:sendOrHold`) as a configurable field in Tune AI → Safety. | S | S |
| 48 | Custom reminder sources | Allow defining custom reminder types (beyond streak-saver, comeback, etc.) with custom labels and timing rules. Stored in global config. | L | S |

### AI quality control

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 49 | Ratings history view | Add a "Ratings" section in customer settings showing all 👍/👎 entries from `js/firebase-db.js:/aiRatings`. Each entry shows draft text, rating, and reason. Currently ratings are stored but never surfaced in the UI. | S | L |
| 50 | Aggregate ratings dashboard | In Tune AI → Usage tab, below the token table, show: overall 👍/👎 ratio, most common thumbs-down reasons (word frequency). Computed from the `aiRatings` node across all customers. | M | L |
| 51 | Bad drafts gallery | A "Bad drafts" subtab in Tune AI showing recent thumbs-down drafts with their verbatim reasons. A living anti-example gallery for prompt tuning. | M | M |
| 52 | Side-by-side A/B sandbox | In Tune AI → Sandbox, add a second output panel. Run two different prompt versions simultaneously on the same customer/input and display the outputs side by side. | M | M |
| 53 | Draft edit tracking | When a user edits an AI draft before sending (original ≠ sent text), store a diff entry in Firebase. Surface in the ratings history to identify what kinds of edits are needed. | M | L |
| 54 | Auto-regression on save | After clicking "Save" in Tune AI, automatically run the sandbox on a fixed list of 5 test customers (configurable in global config). Show results in a modal before confirming the save. | L | L |
| 55 | Prompt version history | Store every Tune AI save in Firebase at `globalConfig/history/{timestamp}`. Show a "History" tab listing versions with timestamps. Add a "Restore" button. | M | L |
| 56 | Hold-rate tracking | In Tune AI → Usage, show "Hold rate" — how often auto-send messages were intercepted by the hold window vs. sent. Computed from `holdQueue` entries across all customers. | S | M |
| 57 | Copy-paste rate tracking | Track whether the user clicked Send with an AI draft unmodified, modified, or after discarding. Store in Firebase as a `sendMode` field alongside the activity log. | M | M |
| 58 | Sandbox batch run | In Tune AI → Sandbox, a "Run all customers" button that runs the current prompt on every configured customer and displays a table of all outputs. Useful for spotting consistency issues. | L | L |
| 59 | Flag for review | Add a "🚩 Flag this draft" button in the `#pending-draft-banner`. Stores the draft in a Firebase `/flaggedDrafts` queue for later analysis rather than discarding silently. | S | M |
| 60 | Temperature override | Add a temperature slider (0.0–1.0) in Tune AI → Safety, stored in global config. Worker reads it in `callAnthropic` and passes it to the Anthropic API. | S | M |

### Workflow & productivity

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 61 | Keyboard shortcut map | Press `?` to show a shortcut overlay: Ctrl+J (coach draft), Ctrl+K (open search), ↑↓ (navigate customers), Enter (open selected customer), Escape (close modal). | S | M |
| 62 | Bulk pause/unpause | Checkbox-select multiple customers from the sidebar and apply pause/unpause in bulk. Currently requires opening each customer's settings individually. | M | M |
| 63 | Copy phone to clipboard | One-click button next to the phone number in customer settings modal. Uses `navigator.clipboard.writeText`. | S | S |
| 64 | Recent customers quick access | Store last 5 visited phones in `localStorage`. Show them as quick-access tabs at the top of the sidebar above the full list. | S | M |
| 65 | Auto-refresh on tab focus | Add a `visibilitychange` listener in `js/chat.js`. When the tab becomes visible after >60s of being hidden, trigger `refreshMessages`. Reduces stale state. | S | M |
| 66 | Dark mode | Add CSS variables toggle for dark mode. A 🌙 button in the header saves the preference to `localStorage`. Useful for evening use by Rohit. | M | S |
| 67 | Batch discard drafts | "Discard all drafts" button in the sidebar header (with confirmation). Iterates all customers with `hasDraft: true` and calls `clearPendingDraft` for each. | S | M |
| 68 | Multi-tab warning | Detect if the dashboard is open in >1 tab (via `BroadcastChannel` or `localStorage` + storage event). Show a warning banner: "Dashboard open in another tab — Firebase writes may conflict." | S | M |
| 69 | Print conversation | "Export conversation" button in the chat header that formats the visible messages as plain text and opens a print dialog. Useful for keeping notes before a call. | S | S |
| 70 | Intent pre-fill hint | Make the intent-to-draft flow more discoverable. Add placeholder hint in textarea: "Type an intent first, then click 💪 to coach or 💬 to reply." Show a tooltip on first use. | S | M |
| 71 | Undo send | After clicking Send, show a 5-second toast with "Undo". If clicked, attempt Periskope message deletion (if supported by API). Prevents accidental sends. | L | M |
| 72 | Notes quick-view | On hover of the `.cust-row`, show the customer's `notes` as a tooltip or hover card if non-empty. Currently notes are only visible inside the customer settings modal. | S | M |

### Analytics & reporting

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 73 | Per-customer message volume chart | In customer settings, show a bar chart (last 4 weeks) of messages sent per week. Uses the activity log in Firebase. Identifies customers getting too many or too few messages. | M | M |
| 74 | Response rate tracker | Track the ratio of outbound messages that receive a customer reply within 24 hours. Store as `responseRate` aggregated in Firebase. Surface per-customer in settings and globally in Tune AI → Usage. | L | L |
| 75 | Habit score sparkline | Show a 14-day sparkline of habit scores on the customer settings Status card, built from `userHabitHistory` data. Currently only the current number is shown. | M | M |
| 76 | Cost breakdown by customer | Attribute Anthropic API cost to individual customers. Store token usage per customer per day alongside the global `aiUsage` node. Surface in Tune AI → Usage. | M | M |
| 77 | Draft acceptance rate | Track and display the percentage of AI drafts accepted (sent unchanged) vs edited vs discarded per day in Tune AI → Usage. | M | L |
| 78 | Automation ROI view | A summary widget: "X messages sent automatically this week, Y customer replies triggered, Z hours not spent on manual messaging." Synthesized from the activity log. | L | M |
| 79 | Error rate trend | Show the Anthropic error count as a 14-day line chart in Tune AI → Usage. Currently only a number in the table. | S | M |
| 80 | Top response hours | Per customer, show which hours of day they most often reply (from activity log timestamps). Surfaces the ideal `sendTimeIST` dynamically. | M | M |
| 81 | Weekly summary panel | A "This week" summary widget at the top of Tune AI → Usage: total messages sent, total drafts generated, acceptance rate, total cost, error count. | M | M |
| 82 | Dormant rate over time | Track the percentage of customers who are "dormant" (5+ days since training) each week. Show as a trend. Measures the health of the customer base. | M | L |
| 83 | Ferra workout trend | For Ferra customers, show a mini chart in customer settings of workout frequency over the last 8 weeks, computed from `userMonthlySummaries`. | M | M |
| 84 | Engagement heatmap | Across all customers, show a heatmap of which hours of the week produce the most customer replies. Helps optimize send times at a fleet level. | L | M |

### Safety, compliance, observability

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 85 | Webhook health monitor | A widget in the Automation feed showing "Last webhook received: Xs ago". Turn red if > 10 min. Detects silent Periskope delivery failures proactively. | S | L |
| 86 | Daily safety summary | At the top of the Automation feed, show today's totals: X messages sent, Y blocked by daily cap, Z blocked by quiet hours. Computed from the automation feed events. | S | M |
| 87 | Opt-out log / prominent indicator | When a customer triggers auto-pause (opt-out keyword), show a red `⛔` indicator on their customer row — not just the ⏸ pill. Add an entry in a global `/optOutLog` node for compliance. | S | L |
| 88 | Worker health check button | In Settings, a "Ping worker" button that calls `/health` and shows response time + "ok"/"error". Detects worker outages before they cause missed messages. | S | M |
| 89 | Audit log on delete | When a customer is deleted, log the event in a top-level Firebase `audit` node (not the customer's node, which is deleted). Store: phone, timestamp, and which customer in settings triggered it. | S | M |
| 90 | Error alerting banner | When the automation feed has 2+ errors in the last 30 minutes, show a red banner at the top of the sidebar. Currently errors are only visible if the user scrolls the automation feed. | S | L |
| 91 | Webhook signature verification | Add a `WEBHOOK_SECRET` Cloudflare env var. Worker verifies `x-periskope-signature` header before processing. Currently any POST to `/periskope/webhook` is accepted without verification. | M | L |
| 92 | Periskope rate limit tracking | Log 429 responses from Periskope into the automation feed. Show a "rate-limited" entry. Currently the error is thrown but not tagged specifically as a rate limit. | S | M |
| 93 | Audit log CSV export | "Export activity log" button in customer settings that downloads the full activity log as CSV. Useful for compliance review. | S | S |
| 94 | Consecutive-outbound safety | In `worker/worker.js:processInboundReply`, check if the last 2 messages in the thread are both `from_me: true`. If so, skip the auto-reply and log `ignored: consecutive-outbounds` to avoid double-texting. | S | L |
| 95 | Quiet hours override double-confirm | When sending a manual message during quiet hours (`safety.js:checkOutboundSafety` returns a warning), require a checkbox + "I understand" text rather than just a `confirm()` dialog. | S | M |
| 96 | Message deduplication audit | In the Automation feed, show the duplicate message_id when `ignored: duplicate` is logged. Currently only the reason is shown, making it hard to identify which message was duplicated. | S | S |

---

## Prompt Engineering (count: 98)

### Persona consistency

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 1 | Rohit's speaking patterns library | Add 10 example sentence starters/endings that match Rohit's actual WhatsApp style: "kal training?", "kya scene hai", "solid session", "abhi toh nahi", "chalo kar lete hain". Paste as a style reference block in the coach system prompt in `js/defaults.js`. | S | L |
| 2 | Founder bio anchor | Add a 2-line "About Rohit" section to the system prompt: "You are Rohit Patel, founder of Ferra (smart resistance-training machine for Indian homes). You personally coach customers on WhatsApp." Grounds the persona in specifics beyond "you are Rohit." | S | M |
| 3 | Corporate language blocklist | Add: "You are NOT a customer service bot. Never use: 'journey', 'achieve your goals', 'unlock your potential', 'transform', 'wellness'. If you catch one of these words, rewrite." Prevents marketing drift. | S | L |
| 4 | Tone range calibration | Define a 1-5 scale in the system prompt: 1=hard ("skipping again?"), 3=default (warm, direct), 5=gentle (empathetic). Add rule: "If daysSinceLastSession > 10, go to 4. If complaint detected, go to 5." | M | L |
| 5 | Anti-list rule | Add: "Never use bullet points or numbered lists. WhatsApp messages are prose. If you need to say two things, use 'and' or a line break." LLMs default to lists in accountability contexts. | S | M |
| 6 | Persona self-check instruction | Add as the final system instruction: "Before outputting, ask: does this sound like a WhatsApp text from a founder-friend? Or like a corporate wellness app? If the latter, rewrite in one sentence." | S | M |
| 7 | Emoji policy tightening | Strengthen the current "no emojis unless they used them first" rule: "Never use emojis in the first message. After they use emojis, use at most 1 per message. Never use 💪 or 🔥 — they signal a bot." | S | M |
| 8 | Name usage policy | Add: "Use their first name at most once per message. Never open with their name alone ('Rajesh, ...'). If the conversation is warm and ongoing, skip the name entirely." | S | S |
| 9 | Approved Hindi word bank | Expand beyond "kal, aaj, thoda": add "theek hai" (okay), "kal pakka" (definitely tomorrow), "yaar" (only if they use it), "bilkul" (absolutely), "kuch toh chal" (something's happening). Prevents over-use or wrong register. | S | M |
| 10 | Sign-off prohibition | Add: "Never end with 'Cheers!', 'Best!', 'Warmly!', or any sign-off. These are WhatsApp texts, not emails." LLMs naturally add sign-offs when prompted for "messages." | S | M |
| 11 | Specificity enforcement | Add: "If the workout data shows a specific number (8 exercises, 42 min) — cite it. Never say 'great session' without stating what made it great." Makes the model use the data it was given. | S | L |
| 12 | Identity negative examples | Paste 5 phrases Rohit would NEVER send: "I hope this message finds you well!", "As your accountability partner,", "Let's crush it!", "I'm here to support your fitness journey!", "Remember, every day is a new opportunity." | S | L |

### Context engineering

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 13 | Inject customer notes | The `config.notes` field is stored in Firebase but is NOT passed to the AI in `js/prompt.js:buildFerraPrompt` or `buildGymPrompt`. Add: "Operator note about this customer: ${config.notes}" if non-empty. This is the highest-leverage, lowest-effort prompt change available. | S | XL |
| 14 | Workout export freshness signal | Add to user prompt: "Workout export fetched at: ${fetchedAt}. If > 6 hours old, the data may not reflect today's session." Prevents the AI from acting on stale data as if it were real-time. | S | M |
| 15 | Weekly summary line over raw 14-day log | Replace or supplement the 14-day daily log with: "This week: 2 sessions (Mon, Wed). Last week: 4 sessions. Trend: declining." More signal, fewer tokens, better model reasoning. | S | L |
| 16 | Stale conversation signal | Add: "Time since their last message to you: ${daysSinceLastReply} days." This prevents drafting a "just saw your message" reply when the conversation is cold. | S | L |
| 17 | Goal injection | If `config.goal` exists (see Dashboard idea #10), add: "Their stated goal: ${config.goal}." Grounds every message in the customer's own motivation. | S | L |
| 18 | System-message filtering in chat history | In `js/prompt.js`, filter out messages where `m.message_type !== 'chat'` and `m.body` is empty before sending to the AI. Media placeholders like `[image]` add noise without meaning. | S | M |
| 19 | Habit score trim to 7 days | Current prompts send 14 days of habit scores. Trim to 7 days (the most recent week). Reduces user prompt tokens by ~40% with minimal information loss. | S | M |
| 20 | Prefer recent activity in prompt order | In `buildFerraPrompt`, reverse the 14-day activity list order: show last 3 days first, then summarize older days as "before that: N sessions over the previous 11 days." Moves most-relevant data to the top of the model's attention window. | S | M |
| 21 | First-message framing | When chat history is empty (new customer), add: "No prior conversation exists. Do NOT draft an opener that duplicates the intro message they already received. Ask about their training setup." | S | M |
| 22 | Diff-based context for cron | For the morning cron check-in, add: "Since your last message to them (${daysSinceLastOutbound} days ago): trained on ${recentDates}. No sessions on ${gapDates}." More informative than raw history. | M | L |
| 23 | Segment-aware framing | Add a conditional block: if `segment === 'at-risk'`, add "Training declining for 2+ weeks — re-engagement is the goal." If `segment === 'dormant'`, add "5+ day gap — lead with empathy, not accountability." | S | L |
| 24 | Exclude cancelled-user note | If the Ferra export's `cancelledUsers` array contains this customer, add: "Note: this customer is in the cancelled users list — their machine may be returned. Avoid references to current machine use." | S | M |

### Anti-hallucination & grounding

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 25 | "Only these facts" instruction | Add: "The ONLY facts you know about this customer are what's in this prompt. If you're about to write something and it's not written above, you don't know it for THIS conversation." Tightens the existing anti-hallucination rule. | S | L |
| 26 | Data absence interpretation | Add: "If no workout is logged for a day, it means no session was recorded. It does NOT mean they intentionally rested. Never say 'good rest day' — you don't know why they didn't train." | S | L |
| 27 | Streak state explanation | Add: "streak.active: false means the streak BROKE — it is gone. streak.days is the length of the BROKEN streak. Do NOT say 'keep up the streak' or 'continuing your streak' if streak.active is false." | S | L |
| 28 | Temporal grounding | Add to user prompt (not system): "Today is ${todayStr}. 'Yesterday' = ${yesterdayStr}. 'Last week' started ${mondayStr}. Never reference any date in the future as if it already happened." | S | M |
| 29 | Tier label meaning | Add to system prompt: "Tier labels mean: Tier 1 < 1 session/week, Tier 2 = 1-2/week, Tier 3 = 3-4/week, Tier 4 = 5+/week. Reference tiers in these terms, not by the label number alone." | S | M |
| 30 | No family/life inference | Add: "You have NO knowledge of this customer's family, job, commute, city, or personal finances unless explicitly stated in the visible chat. Never infer these from their name, phone prefix, or language." | S | M |
| 31 | Silence interpretation rule | Add: "If the customer hasn't replied in X days, do NOT say they're 'busy' or 'having a tough week'. You don't know why. Ask directly: 'kya scene hai?' is better than assumptions." | S | L |
| 32 | Workout count sanity | Add: "If the workout log shows > 20 exercises in one session, this may be a data anomaly — don't lead with the exercise count. Focus on duration and progressive overload sets instead." | S | S |
| 33 | Post-workout timing grounding | Add: "If the post-workout reminder reason says 'finished about X min ago' — use that timing. Do NOT say 'you just finished' if the session was hours ago. Match the language to the timing context in the reason." | S | L |
| 34 | Confirmation-before-personal-detail rule | Add: "If you want to mention something personal (their schedule, their gym name, their specific goals) that is NOT explicitly written above, ask instead of stating. 'What does your week look like?' beats 'I know you're busy on Tuesdays.'" | S | L |
| 35 | No invented workout day names | Add: "Do NOT name workout types (chest day, leg day) unless the customer has used those names in the visible chat above. Use generic terms (upper body, lower body) instead." | S | M |
| 36 | Progressive overload count sanity | Add: "progressiveOverloadCount is the number of sets where resistance increased vs the previous session at that exercise. 1-3 is normal; > 5 in a single session is unusual. Don't over-celebrate if the count seems anomalous." | S | S |

### Output format constraints

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 37 | Hard character count instruction | Add: "Your response must be between 20 and 150 characters. If over 150 characters, cut to the single most important thing to say." Current "1-3 sentences" guidance is enforced inconsistently. | S | L |
| 38 | No-question opener rule | Add: "Never start with a question. Lead with an observation or statement, then optionally end with one question." Prevents the generic "How's training going?" opener. | S | L |
| 39 | One question per message | Add: "End with at most ONE question. If you have two things to ask, pick the more important one and drop the other." Multiple questions get answered selectively or not at all. | S | M |
| 40 | No WhatsApp markdown | Add: "No *bold*, no _italic_, no ~strikethrough~. Plain unformatted text only. WhatsApp renders markdown but it looks mechanical in personal messages." | S | M |
| 41 | Anti-preamble examples | Add 5 bad preamble patterns to the system prompt: "Sure, here's a message:" / "Of course, I can help:" / "Here's what I'd say:" / "Happy to draft this:" / "Here's a draft:". Show the corrected version next to each. | S | L |
| 42 | Acknowledgment compression | Add: "If you need to acknowledge their previous message, do it in half a sentence. Do NOT repeat back what they said. 'Got it' > 'I understand you said that you trained legs today.'" | S | M |
| 43 | No ellipsis rule | Add: "Never end a message with '...' — it reads as passive-aggressive or uncertain in a coaching context." | S | S |
| 44 | One-exclamation-mark cap | Add: "Limit exclamation marks to 1 per message maximum. Most accountability messages need zero." | S | S |
| 45 | Paragraph break rule | Add: "If you have two distinct thoughts, separate them with a line break — don't chain them with 'and' or 'but'. WhatsApp renders line breaks; they make messages scannable." | S | M |
| 46 | Media-message response format | Add: "If the customer's last message is media with no text body ([image] / [audio] / [voice_note]), acknowledge it in half a sentence ('Got your voice note') and pivot to training." | S | M |
| 47 | Language detection rule | Add: "If the last message from the customer is in Hindi (Devanagari or Hinglish), reply in Hindi/Hinglish. If in English, reply in English. Match their dominant language from their most recent message." | S | L |
| 48 | Consecutive-outbound suppression | Add: "If the last message in the chat is from_me (you sent it) and the customer hasn't replied, DO NOT draft another outbound message. The last line of the chat context shows this." | S | L |

### Few-shot examples

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 49 | Active Ferra customer example | Add to system prompt: "GOOD: [user trained yesterday, 6 exercises, 35 min] 'Solid session yesterday — 35 min and you didn't skip on a weekday. Same energy today?' BAD: 'Great job training yesterday! Keep it up!'" | S | L |
| 50 | Dormant 6-day example | Add: "GOOD: [6 days since training] 'Kal 6 din ho gaye — kya scene hai?' BAD: 'I noticed you haven't been training lately. I hope you're doing okay!'" | S | L |
| 51 | Workout report acknowledgment example | Add: "GOOD: [customer says 'done legs today'] 'Legs done, solid. Pull day tomorrow?' BAD: 'That's amazing! Keep up the great work on your fitness journey!'" | S | L |
| 52 | Off-topic pivot example | Add: "GOOD: [customer asks 'kya scene hai bhai'] 'All good — tu bata, training kab?' BAD: 'Hi! I hope you're having a great day! I wanted to touch base about your training!'" | S | L |
| 53 | Complaint handling example | Add: "GOOD: [customer says 'machine is too hard'] 'What's feeling hard — the resistance level or the movement?' BAD: 'I completely understand your frustration. The machine is designed to be challenging but rewarding!'" | S | L |
| 54 | New customer (no history) example | Add: "GOOD: 'Morning! Machine set up at home yet?' BAD: 'Hi! As your new accountability partner, I'm so excited to help you begin your fitness journey on the Ferra machine!'" | S | L |
| 55 | Streak-break example | Add: "GOOD: [9-day streak just broke] 'Streak gaya, but 9 days bhai — restart kal?' BAD: 'Don't worry about the streak! Every setback is a comeback opportunity! You've got this!'" | S | L |
| 56 | Progressive overload example | Add: "GOOD: [3 progressive overload sets] 'PR sets in yesterday's session — that's the compound effect. Same weights next time?' BAD: 'Amazing job hitting your progressive overload sets! That's exactly how gains happen!'" | S | L |
| 57 | Gym customer weekly check-in example | Add: "GOOD: [gym customer at 2/4 workouts this week] 'Tu 2/4 hai abhi — kitne din bache hain week mein?' BAD: 'You're doing great at 2 workouts! I believe in you to complete all 4! Don't give up!'" | S | L |
| 58 | Post-workout (just finished) example | Add: "GOOD: [workout 15 min ago] 'Nicely done — kitna laga aaj?' BAD: 'Congratulations on completing your workout today! I'm so proud of your dedication!'" | S | L |
| 59 | Technical question example | Add: "GOOD: [customer asks about cable height adjustment] 'Pull the pin at the arm side and lift to your height — which exercise are you setting up?' BAD: 'Great question! The Ferra machine is designed with user-friendly adjustments to support your fitness goals!'" | S | L |
| 60 | Cold restart example | Add: "GOOD: [last message was 4 days ago by Rohit, customer hasn't replied] [no new message drafted — skip] BAD: [send another 'checking in on you!' message]" | S | L |

### Customer-segment specialization

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 61 | New customer branch (< 7 days) | Add branch in system prompt: "If conversationStartTs < 7 days ago: they're new. Priority 1 is getting them to COMPLETE their first session. Don't reference streaks. Don't cite habit scores. Ask about their setup." | S | L |
| 62 | Active customer branch | Add: "If they've trained 3+ times this week: don't nag or motivate. Celebrate briefly and ask what's next ('pull day tomorrow?'). Your job here is companionship, not motivation." | S | M |
| 63 | Dormant branch (5-14 days) | Add: "If daysSinceLastSession is 5-14: DON'T reference the habit score number. DON'T lead with accountability. Open with curiosity ('kya chal raha hai') before pivoting to training." | S | L |
| 64 | Long-dormant branch (15+ days) | Add: "If daysSinceLastSession > 14: this is churn-risk. Lead with empathy only. Ask what changed. Only ask about training after they engage. One soft question max." | S | L |
| 65 | High-habit-score branch (70+) | Add: "If habitScore > 70: this customer is consistent. Your job is challenge and progression, not motivation. Reference progressive overload, next goal targets, and periodization." | S | M |
| 66 | Low-habit-score branch (< 30) | Add: "If habitScore < 30: don't reference the score number. Focus on the smallest possible next step: a 10-min session, any single exercise. Lower the bar explicitly." | S | M |
| 67 | Weekend branch | Add: "On Sat/Sun: for consistent customers, ask about their weekend plan (lighter tone). For dormant customers, offer a short 10-min restart session as an easy entry point." | S | M |
| 68 | Ferra machine specifics | Add to coach system prompt: "The Ferra machine uses cable-based resistance. Resistance adjusts via a pin mechanism on the cable arm. Exercises include: Cable Pull-down, Cable Row, Chest Press, Shoulder Press, Leg Press, Curl, Tricep Extension. Reference these by name when relevant." | S | L |
| 69 | Gym customer vocabulary | In gym prompt: add "For gym customers, use gym-specific language: barbell, dumbbell, plate, bench. Never mention the Ferra machine or cable pin — they don't have one." | S | M |
| 70 | Post-workout timing branch | Add: "If reminder source is 'post-workout' and reason includes 'just finished': use present-tense congratulation. If 'finished about Xh ago': use past-tense reference ('trained this morning')." Already available in `worker.js:seedPostWorkoutReminders` reason field. | S | L |
| 71 | VIP peer-level tone | Add: "If habitScore > 80 AND membership > 90 days: adopt a peer tone, not a coach tone. They don't need motivation — they need a training partner. Ask about their next goal, not their next session." | S | M |
| 72 | Stated-intent source specialization | When building reminder prompts for source `stated-intent-followup` vs `stated-intent-no-show`, use different framing: followup = "checking in as planned", no-show = "gentle reality check without shaming." | S | M |

### Conversation continuity

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 73 | Cold vs warm start detection | Add to user prompt: "Last message from them: ${daysSinceLastCustomerMessage} days ago. If > 2 days, this is a cold restart — don't continue a previous thread without re-establishing context in one line." | S | L |
| 74 | Last-outbound-message injection | Add the last message sent by Rohit to the context: "Your last message to them: '${lastOutboundMessage}'. Check if they answered what you asked before drafting a new question." Requires storing last outbound message in Firebase config. | M | L |
| 75 | Unresolved question tracker | In the user prompt, flag if Rohit's last outbound contained a question and the customer didn't reply: "You asked '${question}' and they haven't replied. Don't repeat the same question — either drop it or try a different angle." | M | M |
| 76 | Consecutive outbound counter | Add to user prompt: "Your last N outbound messages had no customer reply. If N >= 2, dramatically soften the next message and ask if this is a good time." Computed from the visible chat context. | S | L |
| 77 | Context bridge for cron after gap | For cron check-ins where the last outbound was > 3 days ago: add a bridge instruction: "Start with a brief nod to the gap ('kal se nahi suna') before the check-in topic. Max half a sentence." | S | M |
| 78 | Don't re-ask answered questions | Add: "Scan the visible chat. If a question was asked and visibly answered (e.g., 'what time?' → '7pm'), do NOT ask the same question again." | S | M |
| 79 | Mid-conversation vs cold-start branching | Add: "If the last message in the chat is < 2 hours old: keep it conversational, no need to re-establish context. If > 24 hours old: treat it as a fresh check-in, don't assume they remember the last topic." | S | M |
| 80 | Reply depth control | Add: "If you see 2+ consecutive outbound messages from you with no customer reply, the next message should be qualitatively softer — shorter, warmer, less demanding. Match the pattern you see." | S | L |
| 81 | Stale context signal | When `conversationStartTs` is set, add to prompt: "Note: chat history visible to you starts from ${startDate}. Earlier messages are intentionally excluded — don't reference them." | S | S |
| 82 | Post-send context update | After the worker sends a message, log it to the activity feed. In subsequent cron runs, the most recent outbound is visible in the chat context. Add: "Your last sent message is the most recent 'me' message in the chat above." | S | S |
| 83 | Momentum cutoff | Add: "If you and the customer have already exchanged 4+ messages today (visible in chat), do NOT draft another outbound unless this is a scheduled reminder. Let them close the conversation." | S | M |
| 84 | Cold intro for reminder after long silence | For reminder-source `comeback` (5-10 days dormant), add specific framing: "Start with warmth, not accountability. 'Hey, how are things?' is a better opener than 'You haven't trained in 8 days.'" | S | L |

### Time-of-day & day-of-week awareness

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 85 | Morning tone instruction | Add: "Current time is ${istHm} IST. If before 10:00 AM: messages should be energizing and forward-looking ('aaj kya plan hai?') — not backward-looking ('did you train yesterday?')." | S | M |
| 86 | Afternoon tone instruction | Add: "If 12:00–17:00 IST: check on planned sessions. If they said they'd train at 6PM, ask if they're still on." Helps distinguish morning check-ins from afternoon nudges. | S | M |
| 87 | Evening tone instruction | Add: "If 17:00–21:00 IST: acknowledge the day's training if it happened, or close the day without pressure. Don't create new urgency in the evening." | S | M |
| 88 | Monday framing | Add: "If today is Monday: new week = fresh start. Don't reference last week's failures. 'New week, kya plan hai?' is the right tone." | S | M |
| 89 | Friday push | Add: "If today is Friday: 'one more session before the weekend' is often the right ask for customers at 2/3 or 3/4 of their weekly goal." | S | M |
| 90 | Sunday recap | Add: "If today is Sunday: for gym customers, offer a week summary: 'You hit X/Y sessions this week — next week starts tomorrow.'" | S | M |
| 91 | Indian holiday awareness | Add a hardcoded list of major Indian holidays to the system prompt (Republic Day, Holi, Independence Day, Diwali, etc.) with dates. "If today is a public holiday: acknowledge it briefly ('Happy Diwali!'), then offer a lighter check-in — don't lead with accountability on a festival day." | M | M |
| 92 | Post-workout timing vocabulary | Add: "If the post-workout reminder reason says 'just finished' (< 30 min): use 'just done'. 30-90 min: 'wrapped up earlier'. 2-4h: 'trained this morning/afternoon'. Never use 'just' if the session was hours ago." | S | L |

### Language & localization

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 93 | Hindi detection flag | Add to user prompt: detect if the last 3 customer messages contain Devanagari or common Hinglish markers. Flag: "Customer communicates in Hindi/Hinglish — match their language register." | M | L |
| 94 | Transliteration consistency | Define canonical spellings in the system prompt: "kal" (not kul), "aaj" (not aaj/āj), "bhai" (not bhi), "yaar" (not yaar/yar). LLMs vary their Hinglish spellings inconsistently. | S | S |
| 95 | Formal vs informal Hindi | Add: "Use informal forms (tu/tum), not formal (aap), unless the customer uses aap themselves. Matching formality register is important in Hindi." | S | M |
| 96 | English-only branch | Add: "If the customer has used ONLY English in all visible messages, do NOT introduce Hindi words — match their language preference. Hindi is an option, not a default." | S | M |
| 97 | South India awareness | Add to notes-based routing (see Dashboard idea #3): if the operator tag includes "south-india" or the phone prefix suggests a non-Hindi region, suppress Hindi words in the prompt. | M | M |
| 98 | Code-switch rule | Add: "Code-switch within a sentence ONLY if the customer does it. If they write exclusively in one language, don't mix languages mid-sentence — it reads as inauthentic." | S | M |

### Failure mode handling

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 99 | Off-topic lockout | Add: "If the customer's message is entirely unrelated to fitness (cricket, weather, food, news): acknowledge with exactly ONE sentence (e.g., 'haha nice'). Then pivot. Spend no more than half a sentence on the off-topic." | S | L |
| 100 | Hardware complaint routing | Add: "If the customer describes a hardware/software issue with the Ferra machine: say 'I'll flag this to the Ferra support team — they'll reach out shortly.' Don't attempt troubleshooting. Don't invent solutions." | S | L |
| 101 | Medical advice refusal | Add: "If the customer describes pain, injury, asks about medication, or mentions a specific medical condition: say 'Sounds like something to check with a doctor — once you get the all-clear, we'll plan around it.' Never give medical advice." | S | L |
| 102 | Ghosted conversation response | Add: "If your last 3+ outbound messages had no customer reply (visible in chat): the next message should be qualitatively shorter, warmer, and non-demanding. Change the type of ask entirely, don't repeat." | S | L |
| 103 | Angry/frustrated customer | Add: "If the customer's message has capital letters, multiple exclamation marks, or words like 'frustrated', 'annoyed', 'I told you': lead ONLY with empathy ('Got it, sounds frustrating'). Never defend or explain on the first response." | S | M |
| 104 | Soft opt-out handling | Add: "If the message contains 'not now', 'I'm busy', 'bura mat mano', 'maybe later' but NOT a hard opt-out keyword: do NOT auto-pause. Instead: 'No pressure — ping me when you're ready.'" | S | M |
| 105 | Ambiguous message rule | Add: "If the customer's message could mean two things, pick the more charitable interpretation and verify: 'Sounds like you had a solid session — that right?' beats assuming the negative." | S | M |
| 106 | Billing/refund routing | Add: "If the customer asks about pricing, billing, refunds, or subscriptions: 'Reach out to the Ferra support team for billing — they'll sort it quickly.' Don't discuss pricing in accountability messages." | S | M |
| 107 | Scheduling request routing | Add: "If the customer asks to schedule a demo, call, or visit: 'Let me check and get back to you.' Do not commit to specific times you don't know." | S | S |
| 108 | Consecutive outbound with no reply | In `worker/worker.js:processInboundReply`, count consecutive from_me messages at the end of the message list. If 2+ consecutive and no customer reply: add to skip reasons `consecutive-outbounds-no-reply`. | S | L |
| 109 | Empty message handling | Add: "If their message is [image], [audio], or [voice_note] with no text: say 'Got your [photo/voice note]' (half a sentence) then pivot to training. Never ask 'what did you mean?' for media-only messages." | S | M |
| 110 | Seasonal/festival sensitivity | Add: "During major festivals or national holidays (visible from today's date): tone down accountability, acknowledge the occasion, and keep any ask light ('train when the chaos settles?')." | S | M |

### Eval methodology

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 111 | Golden test set | Define 10 test cases covering: new Ferra customer, active Ferra (trained today), dormant Ferra (8 days), gym customer on track, gym customer behind, complaint message, off-topic message, post-workout ack, streak break, Hindi-only speaker. Store as JSON in `docs/golden_tests.json`. | M | L |
| 112 | Sandbox batch run on golden set | In Tune AI → Sandbox, add "Run golden tests" button that processes all golden test cases with the current prompt and displays a results table. Flag outputs > 3 sentences or containing preamble. | L | L |
| 113 | Rating-driven tuning loop | Monthly: export all thumbs-down ratings from Firebase, group by reason keyword, identify the top 3 failure patterns, and draft 1-2 new anti-examples per pattern. Test in sandbox before saving. Document as a recurring Claude Code routine. | M | XL |
| 114 | Blind A/B comparison | When testing a prompt change: run old and new prompts on all golden test cases, present outputs in random order without labels, ask Rohit to pick the better one blind. Record the winning version. | M | M |
| 115 | Prompt diff changelog | Maintain a `docs/PROMPT_CHANGELOG.md`. Each Tune AI save auto-appends: date, what changed (1 sentence), who saved it. Store the old prompt alongside for rollback context. | S | M |
| 116 | Output length tracking | After each Tune AI save, run the golden tests and compare average output character length vs. the previous version. If a change increases length > 20%, flag it as a potential verbosity regression. | M | M |
| 117 | Hallucination-specific test cases | Add 3 test cases specifically designed to provoke hallucination: (a) customer with no chat history, (b) customer with a 7-day gap in workout data, (c) customer whose name is ambiguous (could be male or female). Check that outputs ask rather than assume. | S | L |
| 118 | Human eval scorecard | A simple Notion table or spreadsheet: for each sandbox output, rate 1-5 on: Specificity, Brevity, Tone match, No-hallucination, Send-readiness. Review after each prompt change. | S | M |
| 119 | Thumbs-down pattern analysis | Export `/aiRatings` across all customers and group by: source (cron / webhook / reminder), customer type (Ferra / Gym), and reason text. Surface: "Gym customers get worse drafts for comeback reminders" as an actionable tuning target. | M | L |
| 120 | Monthly prompt review ritual | Set a recurring Claude Code routine (monthly): export last 30 days of ratings, check the golden test set, and surface a one-page "what to fix" report. Feed directly into the tuning loop. | M | L |

### Prompt versioning & rollback

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 121 | Firebase version history | On every Tune AI save, write the old config to `globalConfig/history/{timestamp}` in Firebase before overwriting the current. The worker always reads `globalConfig/current`. | M | L |
| 122 | History tab in Tune AI | Add a "History" tab showing last 10 versions with timestamps. Each row shows: date, auto-generated 1-sentence diff summary, "Restore" button. | M | M |
| 123 | Snapshot before edit | When the user opens Tune AI, auto-write the current config to `globalConfig/draft_snapshot`. Available as a rollback point even if the user forgets to version manually. | S | M |
| 124 | Version labels | Let the user add a label when saving: "v3 — added gym examples, removed 'journey'". Store with the version entry. Show in History tab. | S | S |
| 125 | A/B prompt experiment | Add `promptExperiment: { variant: 'B', fraction: 0.2 }` to global config. The worker routes 20% of customers (by `hash(phoneKey) % 10 < 2`) to variant B prompts. Logs `variant` in automation events for comparison. | L | L |
| 126 | Prompt character counter | Show live character count below each system prompt textarea in Tune AI. Show a rough token estimate (`chars / 4`). Flag if > 1000 tokens with a warning. | S | M |
| 127 | Export / import prompts | "Export prompts JSON" and "Import prompts JSON" buttons in Tune AI → Prompts tab. Allows offline backup and sharing prompt versions between environments. | S | M |
| 128 | Auto-summary on save | After saving, call Claude Haiku with the diff of old and new prompts: "Summarize this change in 1 sentence." Store as the version's auto-description. | M | M |

### Token efficiency

| # | Title | Description | Effort | Impact |
|---|---|---|---|---|
| 129 | Prompt caching for system prompt | Use Anthropic's prompt caching (`cache_control: { type: "ephemeral" }`) on the system prompt block in `worker/worker.js:callAnthropic` and `js/anthropic.js`. The system prompt changes rarely — caching saves ~75% of system prompt input token costs per call. | S | XL |
| 130 | Haiku for Ferra active-customer cron | In `worker/worker.js:processCustomer`: if `daysSinceLastSession === 0` (customer trained today), use Haiku for the cron check-in — simpler task, lower stakes. Use Opus/Sonnet only for dormant or first-time customers. | S | L |
| 131 | Haiku for sentiment classification | Before building the full prompt in `processInboundReply`, run a fast Haiku call to classify the inbound: "positive / neutral / negative / complaint / opt-out-adjacent". Route the system prompt based on the classification. | M | M |
| 132 | Skip workout fetch for gym customers | In `worker/worker.js:processInboundReply` line ~446, the workout JSON is fetched even for gym-type customers who don't use it. Add an early check: if `customerType === 'gym'`, skip `fetchWorkout(env)`. | S | M |
| 133 | Per-call token cap by mode | Set `maxTokens` per call type: Haiku extraction = 200, reminder drafts = 300, coach/reply drafts = 400. Current flat cap of 600 wastes output budget on simple tasks (`worker.js:callAnthropic:1017`). | S | M |
| 134 | Context guard (2000 token check) | Before the Anthropic call in both worker and `js/anthropic.js`, estimate prompt token count (`chars / 4`). If combined system+user > 2000 tokens, truncate the chat history (not the workout data) to the last 10 messages instead of 20. | S | M |
| 135 | Worker KV cache for workout JSON | Store the workout JSON in Cloudflare KV with a 5-min TTL. Currently relying on `cf: { cacheTtl: 300 }` which is edge-cache only and may miss in some regions. KV is explicit and reliable across cron ticks. | M | M |
| 136 | Lazy workout fetch in dashboard | In `js/customers.js:refresh`, `loadWorkoutData()` is called for every customer on every page load. Defer the fetch until a customer is actually selected — call it in `openChatFor` only. | S | M |
| 137 | Compact activity log archiving | Add a Firebase cloud function or worker cron subtask that archives activity events older than 30 days to a `/archive/customers/{phone}/activity` node. Prevents unbounded growth of activity logs read on every modal open. | M | M |
| 138 | Reduce replay calls | In `js/chat.js:maybeReplayOrphanInbounds`, check if the `webhookEventsByMessageId` map already has an entry before replaying. Currently the check `webhookEventsByMessageId.has(m.message_id)` exists but the replay fires before events propagate — add a 90s minimum age check instead of 60s. | S | M |

---

## Quick wins shortlist (S effort + M+ impact)

1. **Inject customer notes into the AI prompt** — notes are stored in Firebase config but not passed to `js/prompt.js:buildFerraPrompt`. One line of code, massive grounding improvement.
2. **Sort customer list by dormancy** — add a `<select>` sort control to `js/customers.js`. No Firebase change needed, sorts the in-memory `allCustomers` array.
3. **"Needs attention" section** — pin customers with `hasDraft || holdQueue || daysSinceLastSession >= 5` to the top of the sidebar. Uses existing data.
4. **Prompt caching in worker** — add `cache_control: { type: "ephemeral" }` to the system prompt block in `callAnthropic`. 75% cost reduction on system prompt tokens.
5. **Trim habit scores to 7 days** — in `js/prompt.js` and `worker/worker.js`, change `getRecentHabitScores(idx, uid, 14)` to 7. Reduces tokens, no quality loss.
6. **Add consecutive-outbounds check** — in `worker/worker.js:processInboundReply`, count trailing from_me messages. Skip if 2+ consecutive with no reply. Prevents double-texting.
7. **Unread message indicator** — track `lastViewedAt` in localStorage per customer. Show a dot badge on rows where last inbound > lastViewedAt.
8. **Weekly summary line in context** — replace/supplement the raw 14-day log with "This week: N sessions. Last week: M sessions. Trend: X." Fewer tokens, better framing.
9. **Worker health check ping** — a button in Settings that calls `/health` and shows response time. One line of JS + existing endpoint.
10. **AI badge inline expand** — replace the modal in `js/chat.js:showInspectorModal` with an inline collapsible panel below the bubble. Same data, much faster workflow.
11. **Webhook health monitor** — track last webhook timestamp in automation feed state. Show "Last webhook: Xs ago" + red if > 10 min. Uses existing automation feed data.
12. **Ratings history view** — add a Ratings section in customer settings reading from Firebase `/aiRatings`. Surfaces data that's already being collected.
13. **Hard character count instruction** — add a 20-150 character constraint to the system prompt. One sentence added to `js/defaults.js:DEFAULT_SYSTEM_COACH`.
14. **Anti-preamble examples** — paste 5 bad preamble examples + corrections into the system prompt. Eliminates the "Here's a draft:" failure mode immediately.
15. **Auto-refresh on tab focus** — `document.addEventListener('visibilitychange', ...)` in `js/chat.js`. Zero dependencies.

---

## Ambitious bets (L effort but transformational)

1. **Rating-driven tuning loop** — weekly Claude Code routine that reads thumbs-down ratings from Firebase, groups by failure pattern, drafts anti-examples, and prepares a prompt diff for review. Closes the feedback loop between send quality and prompt content.
2. **A/B prompt experiment in worker** — route 20% of customers to a "variant B" prompt via `hash(phoneKey) % 10`. Log variant in automation events. Enables real-world prompt comparison without guessing.
3. **Response rate tracker** — track which outbound messages get a customer reply within 24h. Surface as a per-customer and global metric. Turns the dashboard from a send-tool into a performance-measurement tool.
4. **Sandbox batch regression on golden test set** — run all golden test cases after every Tune AI save. Flag regressions automatically. Requires building the golden test JSON + batch runner in the UI.
5. **Periskope chat sync / import** — fetch the full chat list from Periskope and show it as an importable customer list. Eliminates manual phone entry and discovers customers who already messaged Rohit but aren't in the dashboard.

---

## Top 10 to ship next

Rank-ordered by impact/effort ratio + current pain observed in the codebase:

1. **Inject customer notes into the AI prompt** (S/XL) — already stored in Firebase config, zero friction to add. Every customer with a "shoulder injury" note is currently getting shoulder exercise suggestions.
2. **Prompt caching for system prompt in worker** (S/XL) — add one `cache_control` field to `callAnthropic`. Reduces cost by ~40-50% per call at current usage levels. No behavior change.
3. **"Needs attention" section at top of sidebar** (M/XL) — surfaces the most actionable customers (pending draft, held send, 5+ days dormant) without any new Firebase reads.
4. **Sort customer list by dormancy** (S/L) — the most urgent customer is the most dormant one, but they're buried in a fixed phone-list order.
5. **Consecutive-outbound safety in worker** (S/L) — prevents double-texting when the customer hasn't replied to the last 2 messages. High-risk situation for customer trust.
6. **Hard character count + anti-preamble examples in system prompt** (S/L) — two lines in `js/defaults.js`. Eliminates the "Here's a draft:" and verbose output failure modes immediately.
7. **Ratings history view in customer settings** (S/L) — Firebase is already collecting ratings on every 👍/👎 click. Not surfacing them is a waste of a feedback signal.
8. **Weekly summary line in prompt context** (S/L) — replace 14-day raw log with "This week: N, Last week: M, Trend: X" in `js/prompt.js` and `worker/worker.js`. Fewer tokens, sharper reasoning.
9. **Dormant branch + long-dormant branch in system prompt** (S/L) — two conditional paragraphs in `DEFAULT_SYSTEM_COACH`. Dramatically changes the tone for the highest-churn-risk customers.
10. **Webhook health monitor widget** (S/L) — know within 10 minutes if Periskope stops delivering webhooks, instead of discovering it when a customer complains they're not getting replies.
